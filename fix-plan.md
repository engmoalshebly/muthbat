# الخطة الرئيسية الموحدة لإصلاح نظام «دفتر ديون / مُثبَت»

**المؤلف:** كبير المهندسين المعماريين (Chief_Architect_Integrator)
**التاريخ:** 2026-08-18
**النطاق:** دمج خطط الإصلاح التسع في `fix-plan/01..09` في خطة تنفيذ رئيسية واحدة، مع حسم كل التعارضات بينها بقرارات موثقة.
**المرجعية:** `final-audit-report.md` (287 ملاحظة، 52 حرجة) + تقارير `analysis-reports/` + الخطط التسع المقروءة كاملة.

---

## 1. ملخص تنفيذي

**الهدف:** نقل النظام من حالته الراهنة (مصادقة مكسورة أمنياً، عقد RPC منكسر بالكامل، ترحيل 0012 مشلول، مزامنة أوفلاين فاشلة صامتاً، 19 ميزة وهمية/معطلة، صفر CI) إلى إطلاق تجاري آمن عبر ثلاث بوابات: مرحلة صفر (يحظر الإطلاق) → تجريبي مغلق (5–10 محلات) → إطلاق تجاري.

**الحجم الإجمالي:**

| المقياس | القيمة |
|---|---|
| الخطوات الخام عبر الخطط التسع | 199 خطوة |
| بعد الدمج وإزالة التكرار وإسقاط مسار OTP المخصص | **~140 خطوة** |
| فوري (24 ساعة) | ~24 خطوة مدمجة — جهد ≈ 5 أيام عمل (تتوازى) |
| مرحلة صفر (يحظر الإطلاق) | ~68 خطوة مدمجة — جهد ≈ 60 يوم عمل → **4 أسابيع** بثلاثة مسارات متوازية (باكند/موبايل/بنية تحتية) |
| مرحلة 1 (تجريبي مغلق) | ~33 خطوة — جهد ≈ 25 يوم عمل → 3–4 أسابيع تشغيل تجريبي |
| مرحلة 2 (إطلاق تجاري) | ~14 خطوة — جهد ≈ 10 أيام + فترة مراقبة 2–4 أسابيع |
| **المدة المتوقعة الإجمالية** | **10–12 أسبوعاً** بفريق 3–4 مهندسين |

**الحكم:** الإطلاق التجاري الفوري مستحيل؛ لكن المسار أدناه يفتح «شرايين» النظام (عقد RPC + مصادقة حقيقية) خلال الأسبوع الأول، ويصل إلى تجريبي مغلق آمن خلال 4–5 أسابيع، وإلى إطلاق تجاري محكم خلال 10–12 أسبوعاً. أي انحراف عن بوابات go/no-go في قسم 6 = No-Go.

---

## 2. القرارات الرئيسية المحسومة

القرارات 1–5 هي **قراراتي أنا ككبير معماريين لحسم التعارضات بين الخطط** (موثقة صراحة). الباقي قرارات حسمها المخططون المتخصصون وأعتمدها هنا كمرجع ملزم.

| # | القرار | الخيار المعتمد | المبرر | الخطة المرجعية |
|---|---|---|---|---|
| **1** | **تعدد العملات (حسم تعارض 01 مقابل 02/05/06)** | **تعايش مرحلي:** تنفيذ إعادة تصميم خطة 01 كاملة (الترحيل البديل 0013: `additional_currencies`، تريجر الأرصدة المصحح، عملة اليومية، views مجمعة بالعملة) **كقدرة خاملة في مرحلة صفر** — الافتراضي `additional_currencies='{}'` يجعل السلوك مطابقاً تماماً لعملة واحدة. واجهة الموبايل تبقى **مقفلة على عملة المحل** (قرار 02 §1.2-هـ و06-D3) والعميل **لا يرسل `p_currency_code`** في v1 (الباكند يقبله كمعامل اختياري افتراضي null — تغيير additive آمن وفق قواعد versioning في 02 §4). التفعيل الفعلي: متاجر مختارة في مرحلة 1 (خطوة 01-17)، والتقارير متعددة العملات بأسعار صرف في مرحلة 2 (01-20). | يستوعب الخطتين بلا تناقض: 01 تبني البنية الصحيحة مرة واحدة (تفادي إعادة عمل مضاعفة)، و02/05/06 يحصلون على سلوك عملة واحدة فعلي عند الإطلاق لأن القائمة المسموحة فارغة افتراضياً والمنتقي مقفل. البديل (تنفيذ 02 حرفياً بإبقاء الأعمدة الخمسة فقط) كان سيرمي تصميم 01 المحاسبي الكامل ويعيد كسر العقد عند تفعيل الميزة لاحقاً. | 01 (التصميم) + 02 §1.2-هـ وخطوة 15 + 06-D3 (قفل الواجهة) — **قرار كبير المعماريين** |
| **2** | **مصير الترحيل 0012 المكسور (حسم تعارض 01 مقابل 02/06)** | **حذف الملف + ترحيل بديل جديد بكتلة تنظيف idempotent** (نهج 01)، لا «إعادة الكتابة في المكان» (نهج 02 خطوة 6 و06-B2). يُسبق بخطوات التحقق الفورية الثلاث (سيناريوهات أ/ب/ج لكل بيئة) والإسقاط الطارئ للتريجر المكسور حيث وُجد. | 01 يغطي السيناريوهات الثلاثة كلها (غير منشور/جزئي/مسجل) بينما نهج «الكتابة في المكان» يفترض سيناريو واحداً ويكسر أي بيئة جزئية؛ وكتلة التنظيف idempotent تحصّن الخطة بلا كلفة إضافية. | 01 قسم 2 — **قرار كبير المعماريين** |
| **3** | **مسار OTP (حسم تعارض 03 مقابل 04)** | **Twilio Verify (قناة WhatsApp) عبر Supabase Phone Auth الأصلي** (خيار 04-أ): `signInWithOtp`/`verifyOTP` أصليان، جلسات PKCE حقيقية، `phone_confirmed_at` يُضبط من Supabase. **يُلغى مسار OTP المخصص بالكامل** (03 §2: جدول `otp_challenges` و`phoneProofToken` ودالة `verify-whatsapp-otp` — لا تُبنى)، ويُحذف `whatsapp_otp_service.dart` ودالة `send-whatsapp-otp` وأسرار OpenWA نهائياً بعد التحول. تصميم 03 المخصص يُؤرشف كخطة طوارئ موثقة فقط إن فشل اعتماد قالب Meta. | الجلسات الأصلية تجعل RLS يعمل فوراً وتلغي مسؤولية سكّ جلسات مخصصة الأمنية الدائمة؛ `bootstrap-user-contact` يشترط هاتفاً موثقاً من Supabase أصلاً؛ `config.toml:59-61` مصمم لهذا المسار؛ وخطة 09-D2 مستقلة وتؤيد مزوداً رسمياً. أجزاء خطة 03 غير المرتبطة بـ OTP (تدوير الأسرار، IDOR، تخويل الرفع، rate limiting، CORS) تبقى معتمدة كاملة. | 04 قرار 1 (+09-D2 تأييداً) — **قرار كبير المعماريين** |
| **4** | **إضافة العميل (حسم تعارض 02 مقابل 06)** | **Edge Function `customer-directory`** (خيار 06-D4) هي المسار الوحيد. **لا تُبنى** دالة `create_business_customer` الجديدة (02 §3 — تُسقط خطوة 02-9). تُحفظ من 02/05 آليات الطابور وتسوية المعرّفات (`local_id_map`، `depends_on_client_request_id`) لكن الأمر المؤجل أوفلاين يصبح استدعاء `customer-directory` عبر `EdgeGateway` عند عودة الاتصال (نمط 06-5.6 `pending_directory`). | `customer-directory` تحل سلسلة C-5 كاملة ذرّياً (تطبيع E164، بحث hash، إنشاء `customers`، تشفير الهاتف في `private.customer_contacts`، ربط العلاقة، طلب ربط اختياري) بينما RPC المباشر كان سيفشل أصلاً لأن العميل لا يملك `customer_id`، وكان سيترك الهاتف (PII الجوهرية لميزة الربط) بلا مسار تشفير. | 06-D4 وخطوة 5.6 (+02 §3.4 للطابور والتسوية) — **قرار كبير المعماريين** |
| **5** | **ترقيم الترحيلات الجديدة (حسم تضارب أرقام 0013 بين 01/02/03/07/09)** | تسلسل موحد استرشادي يُثبَّت عند التنفيذ: `0013` = multi_currency_redo + توحيد عقد RPC (مدمج من 01+02) · `0014` = الأقفال المالية والإيصالات (07 S1–S6, S8) · `0015` = اتساق الكشوفات (07 S7) · `0016` = تسوية AR (07 S9) · `0017` = إصلاحات Storage (03-4.3) · `0018` = `profiles.user_type` (04-1.2) · `0019` = جدولة العمال (09-19). ترحيل OTP (03-2.1) **أُسقط** مع القرار 3. | خمس خطط حجزت الرقم 0013؛ بلا تسلسل موحد ستتصادم الترحيلات عند الدمج. الترتيب يحترم الاعتماديات: العقد قبل الأقفال (S6 يضيف معاملات للدوال نفسها) قبل الجدولة. | دمج 01/02/07/03/04/09 — **قرار كبير المعماريين** |
| 6 | عقد RPC الموحد v1 | جدول 02 §1.1 (16 دالة) هو المرجع الملزم الوحيد؛ توسيع `create_ledger_entry` خلفياً بمعاملات افتراضية (تصنيف/سداد/مرفق) مع اشتقاق العملة من المحل؛ تصحيح العميل للأسماء الأربعة `command_*` بلا wrappers خلفية. | الدوال العامة موجودة وممنوحة ومختبرة؛ التوسيع additive لا يكسر أحداً؛ wrappers تضاعف سطح API. | 02 §1 (+01 §3.5 للتوقيع الموسع) |
| 7 | إصلاح حل النزاع الجزئي على قيود الخصم | المالك الوحيد: خطة 07 خطوة S2 (تفريع `discount` إلى `command_apply_customer_discount`). خطوة 02-8 تشير إليها فقط ولا تملكها. | منع ازدواجية التنفيذ على نفس الدالة `command_resolve_dispute`. | 07-S2 (+02 خطوة 8 إحالة) |
| 8 | idempotency الشاملة | تعميم `command_receipts` على الأوامر الثمانية في **مرحلة صفر** (07-S6) — يستوعب ويقدّم خطوة 02-16 (idempotency النزاعين) التي كانت مؤجلة لمرحلة 1. | طابور الأوفلاين يعيد إرسال أوامر النزاع؛ تأجيل الإيصالات يفتح نافذة تكرار رسائل/حلول أثناء التجريبي. | 07-S6 (يستوعب 02-16) |
| 9 | نمط القفل الموحد | ترتيب أقفال ثابت (idempotency استشاري ← صف `business_customers` ← القيد ← قفل النزاع) في كل الأوامر المالية. | يمنع تسابق «سداد+عكس» = رصيد سالب، وdeadlock بين الدوال المركبة. | 07-D1 |
| 10 | سياسة «الدفع الأكبر من الرصيد» | **السماح افتراضياً** (رصيد دائن «له مبلغ») مع مفتاح لكل محل `allow_customer_credit_balance`؛ العكس يخضع لنفس المفتاح؛ تحديث `First-version.md` المتناقض. | التنفيذ والاختبار والوثائق الحديثة كلها على السماح؛ السيناريو التجاري (سلفة) حقيقي. | 07-D4 + 09-24 |
| 11 | مصدر الحقيقة للرصيد | `ledger_entries` تشغيلياً + الدفتر العام مرآة محاسبية + تسوية آلية يومية تكشف الانحراف خلال 24 ساعة. | الأبعاد التشغيلية (استحقاق/تأكيد/نزاع) لا توجد في GL؛ الذرّية مضمونة بالتريجر. | 07-D3 |
| 12 | بيئات العمل | محلي (CLI) + مشروعان سحابيان `staging`/`prod` فقط؛ حقن الإعداد عبر `--dart-define` و`APP_ENV`؛ حذف `offline_mock_mode` وأي تبديل خادم من داخل التطبيق. | عزل بيانات حقيقية بأقل كلفة؛ منع اتصال بناء الإنتاج بـ localhost. | 09-D1 + 04 §6 |
| 13 | ترحيل المستخدمين التجريبيين | **مسح كامل (Clean Slate)** + مسح SQLite/SharedPreferences عند أول تشغيل للبناء الجديد. | لا بيانات مالية مزامنة تُفقد (المزامنة لم تنجح قط)؛ إصلاح هويات مفبركة أخطر من إعادة تسجيلها. | 04 §7 |
| 14 | عربية PDF | البقاء على pdf-lib + تضمين خط عربي TTF عبر fontkit + reshaping/bidi + تخطيط RTL. | يحافظ سلسلة التوقيع والتحقق العام (SHA-256 + `verify-statement`) بلا تبعية خارجية. | 09-D7 |
| 15 | جدولة العمال | `pg_cron` + `pg_net` بسر `WORKER_SECRET` في Vault لاستدعاء العاملين كل دقيقة/5 دقائق. | البنية مثبتة ومعطلة؛ البديل (GitHub Actions cron) خارج infra-as-code. | 09-D5 |
| 16 | المراقبة | Sentry (Flutter + Deno) + Healthchecks لنبض العمال + فحص سلامة أرصدة يومي. | تغطية واحدة للطرفين؛ مطلوبة صراحة في وثيقة المنتج. | 09-D6 |
| 17 | versioning العقد | تطور additive فقط؛ التغيير الكاسر بدالة `_v2` مع نافذة دعم دورتين؛ سجل عقد مولّد آلياً + بوابة CI للمانيفست. | PostgREST يطابق بالأسماء والمعاملات الافتراضية اختيارية — additive مجاني وآمن. | 02 §4 |
| 18 | حل تعارضات المزامنة | Server-wins مطلق للقيود والأرصدة؛ LWW بـ `updated_at` للحقول الوصفية فقط مع حماية `pending`؛ dead-letter بعد 8 محاولات بلا حذف صامت. | منتج مالي: السيرفر مصدر الحقيقة المحاسبية؛ الفقدان الصامت كارثة ثقة. | 05 رابعاً/ثالثاً |
| 19 | مرفقات القيود | خط أنابيب «بعد المزامنة»: حفظ محلي بـ `client_request_id` ثم رفع عبر `signed-document-upload` بعد معرفة `entry_id` الخادمي؛ `p_attachment_path` يُسقط من الحمولة (يُقبل `p_attachment_url` مؤقتاً null). | الرفع يشترط كياناً خلفياً موجوداً؛ القيد أوفلاين-أول لا معرف خادمي له عند الإنشاء. | 06-D5 (+02 §1.2-أ) |
| 20 | اختبارات RLS السلبية | ملفات pgTAP جديدة مخصصة (010–014) بقالب «مستأجران A/B + sanity إيجابي»؛ كل خلية Denied في rls-matrix تتحول لاختبار قبل الإطلاق. | في نظام مالي متعدد المستأجرين «ماذا يُمنع» يسبق «ماذا يعمل». | 08 قرار 1 و§3 |

---

## 3. خريطة المراحل الرئيسية

### 3.أ فوري — خلال 24 ساعة (~24 خطوة مدمجة، ≈ 5 أيام عمل متوازية)

**المسار الأمني (احتواء):**
1. إبطال مفتاح OpenWA المسرَّب لدى المزود + فحص سجلات 90 يوماً + تسجيل المفتاح الجديد في `supabase secrets` (03-1.1 = 04-0.4 = 09-7).
2. إزالة القيم الافتراضية من `send-whatsapp-otp` والفشل الصريح (03-1.2).
3. تنظيف تاريخ git بـ `git filter-repo` + فحص `gitleaks` كامل التاريخ (03-1.3 = 09-8 جزئياً).
4. تفعيل `gitleaks` في CI وpre-commit مع قاعدة نمط `owa_k1_` (03-1.4 = 08-2 = 09-10).
5. جرد الأسرار الشامل واعتماد سجل أسرار موقّع (09-8).
6. رقعة ساخنة: حذف قبول `123456`/`000000` و`mockOtpCode` (04-0.1).
7. تعطيل fallback الكاش في `loginWithPassword` (04-0.2).
8. تعطيل `_checkExistingSession` المعتمد على SharedPreferences (04-0.3).

**مسار قاعدة البيانات (احتواء):**
9. تنفيذ استعلامات التحقق الثلاثة في كل بيئة وتوثيق سيناريو كل منها (01-1).
10. إسقاط طارئ للتريجر المكسور `trg_ledger_entry_currency_balance` حيث وُجد (01-2).
11. حذف ملف 0012 من الفرع الرئيسي (أو تجميده موثقاً في السيناريو ج) (01-3).
12. توقيع ADR قرار تعدد العملات المرحلي (القرار 1 أعلاه) من مالك المنتج والهندسة (01-4 معدّلة بالقرار 1).
13. حسم السياسات المعلقة توثيقياً: الدفع الزائد، voided، مصدر الحقيقة (07-S3 = 09-24 جزئياً).
14. اقتطاع وصف القيد اليومي عند 500 حرف (07-S4).
15. تصحيح ادعاءات الوثائق الخاطئة فور الأثر (`generate-statement`/`process-automation-rules`) (09-21).

**مسار العميل (فتح عقد RPC — بلا اعتمادية خلفية):**
16. تصحيح معامل العكس `p_original_entry_id` → `p_entry_id` (02-1 = 05-1 جزئياً = 06-3.8 جزئياً).
17. حذف `p_currency_code` من حمولة الخصم (02-2 = 06-3.8).
18. تصحيح الاستدعاءات الأربعة `command_*` → الأسماء العامة (`add_dispute_message`، `resolve_dispute`+`p_resolution_note`، `invite_business_member`+`p_target_user_id`، `create_statement`+`business_customer`) (02-3 = 06-3.1/3.2/3.6/3.7).
19. تمركز أسماء RPC في `RpcContract` + lint يمنع `rpc('` الحرفي (02-4).
20. اختبار حمولات Dart الذهبي (02-5 = 08-MOB-U05).
21. إصلاح الاستعلامات المكسورة: تضمين النزاعات (06-3.3)، محادثة النزاع `sender_user_id` (06-3.4)، قائمة الفريق بالأعمدة الفعلية (06-3.5).
22. طبقة أخطاء موحدة `ApiException` — ممنوع `return false` الصامت (06-3.9 = 05 سابعاً جزئياً).
23. حارس signOut ضد فقدان الطابور (05-4 = 04-1.9 مبكراً).
24. بوابة CI الأولى: workflow بمراحل secrets + lint + pgTAP الحالية + `flutter analyze/test` + **`supabase db reset` من الصفر** (يكشف X-1 ويوثقه) (08-1 + 08-3).

> **ملاحظة حاكمة:** خطوة CI (24) ستكون حمراء حتى اكتمال بند 11 (حذف 0012) — هذا متوقع وموثق كدليل انحدار X-1.

### 3.ب مرحلة صفر — يحظر الإطلاق (~68 خطوة، ≈ 60 يوم عمل، 4 أسابيع)

**الترتيب الحاكم بالاعتماديات المتبادلة:** عقد RPC (الترحيل 0013) ← المزامنة ← ربط الميزات · المصادقة ← كل اختبارات E2E · الأقفال المالية S1 ← S2/S5/S6/S7 · أسرار البيئات ← جدولة العمال ← نبض المراقبة.

**المسار 1 — قاعدة البيانات والعقد (الأسبوع 1):**
1. الترحيل البديل `0013`: كتلة تنظيف idempotent + `additional_currencies` + `business_allows_currency` (01-5) + enums التصنيف/السداد (01-6) + جدول `customer_currency_balances` المصحح وRLS (01-7) + تريجر الأرصدة debit/credit وbackfill (01-8).
2. توحيد مسار إنشاء القيود: توسيع `private.command_create_ledger_entry` وإعادة نشر `public.create_ledger_entry` بالتوقيع الكامل (01-9 = 02-7 مدمجتان) + توسيع `apply_customer_discount` بعملة اختيارية (01-10 = 05-2).
3. تعديل `validate_ledger_entry_insert` لعضوية القائمة المسموحة (01-11) + عملة طبقة القيد المزدوج وميزان المراجعة بالعملة (01-12) + إصلاح الـ views الثلاثة و`command_generate_statement` بالفلترة بالعملة (01-13) + دالة المصالحة (01-14).
4. اختبارات pgTAP تعدد العملات (01-15) + إعادة توليد `full_schema.sql` (01-16).
5. الأقفال المالية: S1 عكس آمن (فوري فعلياً) → S2 خصم النزاع → S5 تأكيد/نزاع → S6 إيصالات الأوامر الثمانية → S8 حسم `voided` → S7 كشف متسق بقفل وidempotency → S9 عرض تسوية AR.
6. حزما الاختبارات: pgTAP سلبية RLS (08-4/5/6)، الثوابت المالية وسياسة الدفع الزائد (08-7)، pgTAP العقد 16 دالة (02-12)، بيان العقد + بوابة CI للمانيفست (02-13)، اختبار التكامل الدخاني الشامل بالحمولات الحرفية (02-14)، pgTAP السلامة المالية (07-S10) وحزام التزامن C1–C6 (07-S11).

**المسار 2 — الأمان والمصادقة (الأسبوعان 1–2):**
7. إصلاح IDOR في `generate-statement` (03-3.1) + فصل رمز التحقق عن مسار الملف (03-3.2).
8. تخويل رفع الملفات بدور كتابة عبر RPC (03-4.1) + magic bytes (03-4.2) + حذف SVG من الباقة العامة (03-4.3) + تحقق sha256 (03-4.4).
9. rate limiting بـ IP غير قابل للتزوير + حد لكل رمز + رفع إنتروبيا الرموز (03-5.1) + CORS فشل مغلق (03-5.2) + توحيد الحدود (03-5.3).
10. مصادقة Twilio Verify: إنشاء الخدمة واعتماد قالب Meta وضبط `[auth.sms]` (04-1.1 — **تبدأ فوراً لأن اعتماد Meta على المسار الحرج**) + عمود `profiles.user_type` والتريجر (04-1.2) + إعادة كتابة الإعداد `fromEnvironment` (04-1.3 = 09-3) + إعادة كتابة `auth_controller` كاملة (04-1.4) + حذف `whatsapp_otp_service.dart` (04-1.5) + الشاشات الأربع وشاشة إعادة التعيين (04-1.6) + استدعاء `bootstrap-user-contact` (04-1.7) + حذف/تعطيل `send-whatsapp-otp` وأسرار OpenWA (04-1.8) + حوار الخروج (04-1.9) + مسح المستخدمين التجريبيين (04-1.10) + اختبارات auth (04-1.11).

**المسار 3 — المزامنة والميزات (الأسبوعان 2–4):**
11. إعادة بناء مخطط الطابور DB v3 (05-6) → تصنيف الأخطاء dead-letter (05-3) → إضافة العميل عبر `customer-directory` مع الطابور وتسوية المعرّفات (06-5.6 = 02-10 معدّلة بالقرار 4) → الاعتماديات بين الأوامر (05-7) → سحب القيود وأرصدة العملات (05-8) → حماية pending من الكتابة الفوقية (05-9) → إصلاح الرصيد المحلي للعكس والعملة (05-10) → واجهة حالة المزامنة وشاشة dead-letter (05-11) → اختبارات تكامل e2e لكل أمر (05-12).
12. ربط الميزات: `EdgeGateway` (06-5.1) + `enqueueMutation` العامة (06-5.2) + PDF الكشف وعرضه (06-5.4) + شاشة العميل الحقيقية: الملخص (06-4.1)، البطاقات (06-4.2)، طلبات الربط والرد (06-4.3)، تأكيد القيود (06-4.4) + قفل منتقي العملة (02-15 = 06-D3) + View أسماء الفريق B-1 (اعتمادية باكند من 06).
13. البنية التحتية: مشروعا staging/prod (09-1) + `config.toml` لكل بيئة (09-2) + تنظيف عناوين LAN من العميل (09-3) + خط نشر الترحيلات staging آلي/prod يدوي (09-4) + أسرار البيئات المنفصلة (09-9) + PITR وخطة Pro (09-12) + Sentry موبايل ودوال (09-16/17) + جدولة العمال pg_cron (09-19) + نبض العمال (09-18) + توقيت تذكيرات الأتمتة بتوقيت النشاط (09-20) + توثيق مصير 012 وحسم `cleanup-expired-data` (09-22) + عربية PDF (09-25).
14. اختبارات الدوال: إعادة هيكلة `_shared` (08-8) + وحدات Deno (08-9) + تكامل الدوال الحرجة بما فيه انحدار IDOR أحمر-ثم-أخضر (08-10) + حقن اعتماديات الموبايل (08-11) + وحدات الطابور والمزامنة (08-12) + عقد RPC المنحدِر X-3 (08-13).

### 3.ج مرحلة 1 — تجريبي مغلق (~33 خطوة، ≈ 25 يوم عمل)

- **تفعيل تعدد العملات تدريجياً** لمتاجر مختارة + سحب أرصدة العملات في المزامنة (01-17) + نقل المرفقات إلى منظومة `files` (01-18).
- **العقد:** الدعوة بالهاتف `p_phone` (02-18 = 06-B3/D6) + تثبيت versioning (02-19) + هجرة العميل للتوقيع الموسع (06-D2 مرحلة 1).
- **الأمان:** مقارنة ثابتة الزمن، توحيد `PHONE_KEY_VERSION`، whitelist الأخطاء، تشديد `customer-directory`، pgTAP سلبية شاملة (03-6.1–6.5).
- **المصادقة:** شاشة إعداد المحل الأولى + `create_business` (04-2.1)، حدود OTP إضافية (04-2.2)، رسائل أخطاء مصنفة (04-2.3)، تخزين آمن صريح وكشف root (04-2.4).
- **المزامنة:** القضاء على N+1 (05-13)، السحب التزايدي عبر `sync_checkpoints` (05-14)، أرشفة الطابور عند signOut (05-15)، ضبط `occurred_at` وانحراف الساعة (05-16 — **ملاحظة توحيد:** خطة 02 تحذر أن تشديد `occurred_at` كاسر؛ يُنفذ كفحص خلفي متسامح في مرحلة 1 ويُشدد في v2).
- **الميزات:** فتح نزاع من العميل (06-4.5)، الكشف المجمع (06-4.6)، التحقق برمز الكشف (06-5.5)، `bootstrap-user-contact` للحسابات القائمة (06-5.7)، خط أنابيب المرفقات (06-5.8)، الرد على دعوات الفريق (06-F7).
- **المالية:** جدولة التسوية اليومية وتنبيه المالك (07-S12)، تدقيق الأحداث المحاسبية (07-S13)، إظهار «له مبلغ» في الواجهة (07-S14).
- **الاختبارات:** دورة النزاعات (08-14)، التزامن (08-15)، تعدد العملات (08-16)، تكامل الدوال المتبقي (08-17)، ودجت الموبايل (08-18)، بوابة التغطية 70%/60% (08-19)، فحص صحة staging اليومي (08-20).
- **الحوكمة:** بيانات بذرة staging (09-5)، نكهات البناء (09-6)، تدوير الأسرار الدوري (09-11)، النسخ الخارجي الأسبوعي (09-13)، تدريب الاستعادة (09-14)، فحص السلامة اليومي (09-15)، مانيفست الوثائق وفحص الانحراف (09-23)، قبول PDF البصري (09-26).

### 3.د مرحلة 2 — إطلاق تجاري (~14 خطوة، ≈ 10 أيام + مراقبة)

- minor units لكل عملة وإشعارات بعملة القيد (01-19) + دراسة تقارير أسعار الصرف (01-20 — تُدرس ولا تُنفذ بلا طلب صريح).
- تصميم تعدد العملات الكامل المعماري اللاحق (02-20) + أول تمرين إهلاك (02-21).
- مراجعة كشف `closing_balance` العام، إزالة GET المؤقت، pentest خارجي (03-6.6–6.8 = 04-3.3).
- مراقبة auth وتنبيهاتها (04-3.1) + قناة SMS احتياطية (04-3.2).
- تشفير SQLite (05-17)، كفاءة المزامنة الدورية (05-18)، قياسات المزامنة (05-19)، تعطيل mock نهائياً (05-20).
- أداء التريجرات المالية (07-S15) + تقييم SERIALIZABLE (07-S16).
- E2E موبايل (08-21)، golden tests (08-22)، أداء مالي (08-23)، اختراق storage (08-24).
- لغة الكشف لكل عميل (09-27) + بوابة البند 22 (تشغيل تجريبي 2–4 أسابيع بلا انحراف).

---

## 4. مخطط الاعتماديات بين الخطط

| الخطوة/المخرج | يعتمد على | نوع الاعتمادية |
|---|---|---|
| 02-7 توسيع `create_ledger_entry` | 01-5/01-6 (ترحيل 0013 وenums) — **مدمجتان في 0013** | باكند ← باكند |
| 02-12/13/14 اختبارات العقد والمانيفست والدخاني | 0013 مكتملاً (01-9) | اختبارات ← باكند |
| 05 كاملة (محرك المزامنة) | 02 §1.1 (العقد الموحد) + 0013 المنشور + 04 (جلسات حقيقية — بلاها كل اختبار E2E زائف) | موبايل ← باكند + مصادقة |
| 06 كاملة (ربط الميزات) | 02 (العقد) + 05 (الطابور والتسوية) + 04 (B-4: إغلاق C-9/C-10/C-11) + باكند B-1 (View أسماء الفريق) | موبايل ← عقد + مزامنة + مصادقة |
| 06-5.4/5.5 (PDF والتحقق) | 03-3.1 (إصلاح IDOR) + 09-25 (عربية PDF للإطلاق) | موبايل ← أمن + حوكمة |
| 06-5.6 (إضافة العميل) | 03-5.3 (تشديد rate limit للدليل) — تنسيق لا حظر | موبايل ← أمن |
| 04-1.11 و05-12 و08 (اختبارات E2E) | 04-1.1..1.7 (مصادقة حقيقية) — **المصادقة قبل كل E2E** | اختبارات ← مصادقة |
| 07-S2/S5/S6/S7 | 07-S1 (ترتيب الأقفال الموحد) | باكند ← باكند |
| 07-S9 (تسوية AR) | 07-S8 (ترشيح `status='posted'`) | باكند ← باكند |
| 08-10 (EF-I02 أخضر) | 03-3.1 (إصلاح IDOR) — يُكتب أحمر أولاً عمداً | اختبارات ← أمن |
| 08-16 (اختبارات تعدد العملات) | 0013 مكتملاً — **محظور كتابتها قبله** | اختبارات ← باكند |
| 08-1 مرحلة `db reset` خضراء | 01-3 (حذف 0012) | CI ← باكند |
| 09-19 (جدولة العمال) | 09-9 (أسرار البيئات) + إصلاح `process-automation-rules` (C-2 — فريق الدوال) | حوكمة ← أسرار + دوال |
| 09-18 (نبض العمال) | 09-19 | حوكمة ← حوكمة |
| 09-4 (نشر prod) | 08-1 (CI أخضر) + 09-1/2 | حوكمة ← CI |
| 01-17 (تفعيل العملات مرحلة 1) | 01-9/13 + 05-8 (سحب أرصدة العملات) | باكند ← موبايل |
| 05-16 (انحراف الساعة) | 02 §4 (مسار versioning — التشديد تغيير كاسر) | موبايل ← عقد |
| 04 (حذف `offline_mock_mode`) | 05-20 (تعطيله في المزامنة) — تنسيق متبادل | مصادقة ↔ مزامنة |

**سلاسل حرجة:**
1. `01-1..3` ← `0013` ← `02-12..14` ← `05` ← `06` ← `05-12/08 E2E` (أطول سلسلة تقنية).
2. `04-1.1 (اعتماد Meta — يبدأ يوم 1)` ← `04-1.4` ← `04-1.7` ← كل E2E.
3. `09-1` ← `09-9` ← `09-19` ← `09-18` ← بند go/no-go 14.

---

## 5. الجدول الزمني المقترح (10–12 أسبوعاً)

| الأسبوع | باكند (مهندس 1) | موبايل (مهندس 2) | بنية تحتية/أمن (مهندس 3) | بوابة |
|---|---|---|---|---|
| **يوم 1 (فوري)** | تحقق بيئات 0012 + إسقاط طارئ + حذف الملف + S3/S4 | رقعة OTP + تصحيحات RPC الأربع + RpcContract + طبقة الأخطاء | إبطال OpenWA + gitleaks + جرد أسرار + CI الأولى | شرايين مفتوحة: لا PGRST202، CI يعمل (أحمر موثق على 0012 فقط) |
| **1** | ترحيل 0013 كامل (01-5..14) + pgTAP تعدد العملات | إصلاح استعلامات النزاعات/الفريق + حارس signOut + اختبار الحمولات | Twilio Verify + اعتماد قالب Meta + مشروعا staging/prod + أسرار البيئات | `db reset` أخضر من الصفر |
| **2** | أقفال مالية S1→S6 + S8 + S10 | auth_controller الجديدة + الشاشات + حذف otp service | IDOR + تخويل الرفع + CORS/rate limit + Sentry | تسجيل/دخول/استرداد E2E على staging |
| **3** | S7 + S9 + حزام التزامن C1–C6 + بيان العقد/المانيفست | مخطط الطابور v3 + dead-letter + إضافة العميل عبر customer-directory + الاعتماديات | جدولة العمال + نبض + توقيت التذكيرات + PITR + عربية PDF | مزامنة أوفلاين كاملة (عميل+3 قيود+خصم+عكس) |
| **4** | views/كشوفات بالعملة + مصالحة + دعم B-1 | سحب القيود/الأرصدة + حل التعارضات + واجهة الحالة + شاشة العميل الحقيقية + PDF | pgTAP RLS سلبية + تكامل الدوال + فحص الانحراف | **بوابة مرحلة صفر: قائمة go/no-go كاملة** |
| **5–8** | تفعيل عملات تجريبية + الدعوة بالهاتف + S12–S14 + إصلاحات ناتجة من التجريبي | versioning + مرفقات + كشف مجمع + تحقق برمز + e2e تكامل | بذرة staging + نكهات + نسخ خارجي + تدريب استعادة + تغطية 70% | **تجريبي مغلق 5–10 محلات** |
| **9–12** | S15/S16 + minor units + مراقبة | E2E أجهزة + golden + تشفير SQLite | pentest + SMS احتياطي + مراقبة auth | **إطلاق تجاري بعد بند 22** |

---

## 6. قائمة go/no-go النهائية (مدمجة من 03 §7، 04 §9، 05 تاسعاً، 06 §7، 08 §7، 09 §7)

تُوقَّع بنداً بنداً من قائد هندسي + مالك منتج. **أي «لا» = No-Go.**

### أ. الأمان والأسرار
1. ☐ مفتاح OpenWA القديم مُبطل لدى المزود، سجلاته مراجعة، ولا أثر له في الكود أو التاريخ (`gitleaks` نظيف على HEAD والتاريخ).
2. ☐ لا أسرار ولا عناوين LAN/localhost في `mobile/lib` أو أي TypeScript؛ أسرار staging ≠ prod وكلها عبر `supabase secrets`.
3. ☐ لا توليد/تخزين/تحقق OTP في العميل إطلاقاً؛ OTP خادمي 100% عبر Twilio Verify؛ تفكيك APK لا يكشف سراً أو عنواناً داخلياً.
4. ☐ `generate-statement` بـ `statementId` يعيد 404 لغير الأطراف (اختبار تكامل موثق)؛ مسار PDF بلا `verification_code`.
5. ☐ إرفاق الملفات يتطلب دور كتابة (pgTAP للأدوار)؛ magic bytes مفحوصة؛ SVG مرفوض.
6. ☐ rate limit بـ IP غير قابل للتزوير + حد لكل رمز؛ CORS فشل مغلق بلا `"*"`؛ استدعاء العمال بلا `WORKER_SECRET` = 401.

### ب. المصادقة والهوية
7. ☐ بحث `'123456'|'000000'|mockOtpCode|WhatsAppOtpService|cached_user_id|usr-|biz-` في `mobile/lib` = صفر.
8. ☐ كل حالة `authenticated` تقابلها جلسة Supabase حية؛ لا دخول بلا جلسة؛ RLS يرفض جلسة منتهية 401.
9. ☐ لكل مستخدم جديد: `phone_confirmed_at` + `profiles.user_type` + `customer_contacts` بعد أول دخول.
10. ☐ e2e أخضر على staging: تسجيل تاجر+عميل، دخول، استرداد، خروج، استعادة جلسة.

### ج. المالية والبيانات
11. ☐ `supabase db reset` من الصفر + `supabase test db` أخضران بالكامل (≥ 60 تأكيداً، ≥ 20 سلبياً، كل خلية Denied مغطاة).
12. ☐ حزام التزامن C1–C6 يمر 20 تكراراً؛ إيصالات الأوامر الثمانية تمنع أي تكرار أثر.
13. ☐ كشف متزامن مع قيد رجعي: `opening+debits−credits=closing=مجموع البنود` دائماً؛ مصالحة الأرصدة ترجع صفر انحراف.
14. ☐ لا جمع لعملتين في رقم واحد في أي view/كشف/إجمالي؛ منتقي العملة مقفل على عملة المحل في v1.

### د. المزامنة والموبايل
15. ☐ صفر PGRST202 في سجلات sync؛ سيناريو «عميل + 3 قيود + خصم + عكس أوفلاين» يُزامن كاملاً والأرصدة تطابق.
16. ☐ فشل دائم → dead_letter خلال جولة واحدة + إشعار مرئي؛ signOut مع طابور → لا حذف صامت تحت أي مسار.
17. ☐ pull يجلب قيداً من جهاز آخر ويحدّث أرصدة العملات؛ صفر نص ثابت في `customer_home_screen.dart` (`grep '450,000'` = لا نتائج).
18. ☐ تغطية ≥ 70% على `lib/core/` و≥ 60% على `_shared/` (مرحلة 1).

### هـ. البيئات والتشغيل
19. ☐ مشروعا staging/prod منفصلان؛ نشر prod يدوي الموافقة بعد CI أخضر على staging؛ لا تعديل على ترحيل مرتكب؛ مصير 0012 موثق.
20. ☐ PITR مفعّل + نسخ `pg_dump` خارجي أسبوعي + تدريب استعادة موثق بـ RTO/RPO مقاسين + فحص سلامة أرصدة يومي ينبّه.
21. ☐ Sentry يلتقط أخطاء الموبايل والدوال بوسم البيئة؛ نبض العاملين مراقَب؛ التذكيرات بتوقيت النشاط لا UTC.
22. ☐ الوثائق متطابقة مع الواقع (مانيفست + `full_schema.sql` مولّد آلياً + فحص انحراف في CI)؛ سياسة الدفع الزائد موحدة في الوثيقتين والاختبارات؛ كشف PDF عربي معتمد بصرياً ويجتاز `verify-statement`.

### و. بوابات المراحل
23. ☐ **تجريبي مغلق:** البنود 1–22 كلها + بيانات بذرة staging + نكهات البناء الثلاث.
24. ☐ **إطلاق تجاري:** تشغيل التجريبي 2–4 أسابيع بلا انحراف أرصدة ولا حادثة أمنية؛ تسليم إشعارات ≥ 95%؛ صفر أخطاء حرجة في Sentry لمدة 7 أيام؛ رضا المحلات موثق؛ pentest بلا نتائج حرجة/عالية.

---

## 7. فهرس الخطط التفصيلية التسع

| # | الملف | المالك | النطاق |
|---|---|---|---|
| 01 | `fix-plan/01-migration-012.md` | مخطط إصلاح الترحيل 0012 | تعدد العملات والتصنيفات — الترحيل البديل 0013 |
| 02 | `fix-plan/02-rpc-contract.md` | مخطط توحيد عقد RPC | العقد الموحد v1، versioning، اختبارات العقد |
| 03 | `fix-plan/03-security-secrets.md` | مخطط الأمان والأسرار | تدوير الأسرار، IDOR، تخويل الرفع، rate limiting، CORS (مسار OTP المخصص فيها **مُلغى بالقرار 3**) |
| 04 | `fix-plan/04-auth-system.md` | مخطط المصادقة | Supabase Phone Auth + Twilio Verify، الجلسات، البيئات، ترحيل المستخدمين |
| 05 | `fix-plan/05-offline-sync.md` | مخطط المزامنة | محرك المزامنة، الطابور، dead-letter، حل التعارضات |
| 06 | `fix-plan/06-broken-features.md` | مخطط ربط الميزات | 22 ميزة (F1–F22) من الطرف إلى الطرف |
| 07 | `fix-plan/07-financial-integrity.md` | مخطط السلامة المالية | الأقفال، الإيصالات، الكشوفات، التسوية، اختبارات التزامن |
| 08 | `fix-plan/08-testing-ci.md` | مخطط الاختبارات وCI | 4 طبقات اختبار + خط GitHub Actions + تعريف الجاهزية |
| 09 | `fix-plan/09-launch-governance.md` | مخطط حوكمة الإطلاق | البيئات، الأسرار، النسخ، المراقبة، الوثائق، عربية PDF، go/no-go |

---

*انتهت الخطة الرئيسية — القرارات 1–5 قرارات حسم تعارضات من كبير المهندسين المعماريين؛ كل ما عداها مستمد حرفياً من الخطط التسع المرجعية.*
