# التقرير النهائي الموحد للتدقيق الشامل — نظام «دفتر ديون» (مُثبَت)

**نوع التقرير:** دمج وتوثيق نهائي لعشرة تقارير تدقيق متخصصة قبل الإطلاق التجاري
**نطاق المشروع:** باكند Supabase في `debt-ledger-supabase` وتطبيق Flutter في `mobile`
**المصادر:** التقارير العشرة في `analysis-reports/` (01 إلى 10) — كل ملاحظة في هذا التقرير مصدرها تلك التقارير حصراً، مع الحفاظ على مراجع الملفات وأرقام الأسطر كما وردت فيها.

---

## 1. ملخص تنفيذي (صفحة واحدة)

### حكم الجاهزية العام

**النظام غير جاهز للإطلاق التجاري في وضعه الحالي.** النواة الخلفية (الترحيلات 0001–0011) مبنية باحترافية عالية وتستحق الثقة: سجل مالي append-only، قيد مزدوج متوازن مفروض على مستوى قاعدة البيانات، idempotency بطبقتين، RLS مفعّل على كل الجداول العامة الـ 31، وتشفير سليم لأرقام الهواتف. **لكن** هذا الأساس محاصر بخمس كسور بنيوية تحبطه عملياً: ترحيل أخير (0012) مكسور لا يمكن نشره ويعطّل كل الكتابة المالية لو طُبِّق، عقد API بين الموبايل والباكند غير متطابق يجعل كل العمليات المالية من التطبيق تفشل دائماً، طبقة مصادقة قابلة للتجاوز بالكامل من جهاز العميل، سر إنتاجي (مفتاح OpenWA) مسرّب في الكود وفي حزمة التطبيق، وغياب كامل لـ CI ولاختبارات النفي الأمنية. بصيغته الحالية، التطبيق أقرب إلى نموذج عرض يعمل على SQLite محلي؛ التكامل الفعلي مع الباكند شبه معدوم (تقرير 08).

### أخطر 5 نتائج

1. **الترحيل `202608180012_multi_currency_and_categories.sql` مكسور بنيوياً:** سياسة RLS تستدعي دالة ونوعاً غير موجودين (`has_business_permission`/`business_permission`، سطر 41) فيفشل النشر، ولو طُبِّق لعطّل تريجره كل إدراج قيد مالي في النظام بمقارنة enum بقيم `'in'/'out'` غير الموجودة (الأسطر 51–111) — عطل شامل للوظيفة الجوهرية للمنتج (تقارير 01، 02، 03، 05، 06، 08، 09).
2. **عقد RPC بين الموبايل والباكند غير متطابق:** التطبيق يرسل معاملات وأسماء دوال غير موجودة (`create_ledger_entry` بستة معاملات زائدة، `command_resolve_dispute`، `command_generate_statement`...) فيفشل كل استدعاء بـ `PGRST202` وتبقى كل العمليات المالية في طابور الأوفلاين إلى الأبد بينما تعرض الواجهة أرصدة محلية وهمية (`merchant_repository.dart:129-143,349,367,422,452`؛ `sync_engine.dart:122-127`) — انحراف دائم وصامت بين أرصدة العميل وقاعدة البيانات (تقارير 02، 05، 06، 07، 08).
3. **مفتاح OpenWA API مسرّب وثابت في الكود:** قيمة حقيقية `owa_k1_...` مضمّنة في `send-whatsapp-otp/index.ts:4-5` ومكررة حرفياً في `whatsapp_otp_service.dart:28-29` — أي شخص يطّلع على المستودع أو يفكك APK/IPA يسيطر على جلسة واتساب التجارية بالكامل (تقارير 03، 04، 07، 08، 09، 10).
4. **مصادقة OTP والجلسات تُتحقق على جهاز العميل وقابلة للتجاوز كلياً:** توليد الرمز وتخزينه والتحقق منه محلياً (`whatsapp_otp_service.dart:95-99`)، مع كودي تجاوز ثابتين `'123456'/'000000'` (سطور 196-199)، وقبول `mockOtpCode` (`auth_controller.dart:397`)، وتسجيل دخول بديل بلا كلمة سر من الكاش المحلي (`auth_controller.dart:128-138`)، واستعادة جلسة من SharedPreferences بلا توكن (`auth_controller.dart:71-94`) — لا قيمة أمنية لإثبات الهوية إطلاقاً (تقارير 03، 04، 07، 08، 10).
5. **لا CI ولا اختبارات نفي أمنية ولا اختبارات موبايل:** لا يوجد أي pipeline، ولا اختبار RLS سلبي واحد (عزل المستأجرين جوهر المنتج)، ولا اختبار لأي من Edge Functions التسع، واختبار الموبايل الوحيد smoke test من 15 سطراً مقابل 53 ملف Dart تشمل أخطر منطق في المنتج (طابور الأوفلاين) — أي تراجع أمني أو كسر تكامل يمر دون اكتشاف (تقرير 10، البنود 1–3 و16).

### إحصائية الملاحظات الإجمالية (حسب التصنيف، كما أعلنتها التقارير)

| التقرير | حرج | عالي | متوسط | منخفض | إيجابي |
|---|---:|---:|---:|---:|---:|
| 01 المخطط والقيود | 5 | 5 | 6 | 6 | 10 |
| 02 الدوال والمنطق | 3 | 5 | 6 | 9 | 7 |
| 03 الأمان وRLS | 5 | 3 | 8 | 4 | 10 |
| 04 Edge Functions | 3 | 3 | 6 | 7 | 8 |
| 05 القيد المزدوج | 2 | 3 | 2 | 4 | 11 |
| 06 تعدد العملات | 5 | 5 | 4 | 3 | 7 |
| 07 طبقة بيانات الموبايل | 6 | 9 | 7 | 5 | 6 |
| 08 عقود API | 15 | 7 | 7 | 3 | 4 |
| 09 التوثيق مقابل التنفيذ | 3 | 4 | 10 | 4 | 7 |
| 10 الاختبارات والجاهزية | 5 | 7 | 6 | 2 | 5 |
| **الإجمالي** | **52** | **51** | **62** | **47** | **75** |

**المجموع الكلي: 287 ملاحظة** (212 ملاحظة سلبية + 75 نقطة إيجابية). ملاحظة منهجية: الأرقام معتمدة من إحصاءات كل تقرير كما أعلنها؛ الترقيم التفصيلي داخل تقرير 10 يصل إلى 9 بنود موسومة «عالي» بينما إحصاؤه المعلن 7 — اعتمدنا الإحصاء المعلن.

---

## 2. النتائج العابرة الحرجة (Cross-Cutting Critical Findings)

خمس مشاكل تكررت عبر عدة تقارير بأدلة مستقلة. نجمع هنا كل الأدلة من كل التقارير في مكان واحد.

### X-1: الترحيل 0012 (تعدد العملات والتصنيفات) مكسور بنيوياً على عدة مستويات
# التقرير النهائي الموحد للتدقيق الشامل — نظام «دفتر ديون» (مُثبَت)

**نوع التقرير:** دمج وتوثيق نهائي لعشرة تقارير تدقيق متخصصة قبل الإطلاق التجاري
**نطاق المشروع:** باكند Supabase في `debt-ledger-supabase` وتطبيق Flutter في `mobile`
**المصادر:** التقارير العشرة في `analysis-reports/` (01 إلى 10) — كل ملاحظة في هذا التقرير مصدرها تلك التقارير حصراً، مع الحفاظ على مراجع الملفات وأرقام الأسطر كما وردت فيها.

---

## 1. ملخص تنفيذي (صفحة واحدة)

### حكم الجاهزية العام

**النظام غير جاهز للإطلاق التجاري في وضعه الحالي.** النواة الخلفية (الترحيلات 0001–0011) مبنية باحترافية عالية وتستحق الثقة: سجل مالي append-only، قيد مزدوج متوازن مفروض على مستوى قاعدة البيانات، idempotency بطبقتين، RLS مفعّل على كل الجداول العامة الـ 31، وتشفير سليم لأرقام الهواتف. **لكن** هذا الأساس محاصر بخمس كسور بنيوية تحبطه عملياً: ترحيل أخير (0012) مكسور لا يمكن نشره ويعطّل كل الكتابة المالية لو طُبِّق، عقد API بين الموبايل والباكند غير متطابق يجعل كل العمليات المالية من التطبيق تفشل دائماً، طبقة مصادقة قابلة للتجاوز بالكامل من جهاز العميل، سر إنتاجي (مفتاح OpenWA) مسرّب في الكود وفي حزمة التطبيق، وغياب كامل لـ CI ولاختبارات النفي الأمنية. بصيغته الحالية، التطبيق أقرب إلى نموذج عرض يعمل على SQLite محلي؛ التكامل الفعلي مع الباكند شبه معدوم (تقرير 08).

### أخطر 5 نتائج

1. **الترحيل `202608180012_multi_currency_and_categories.sql` مكسور بنيوياً:** سياسة RLS تستدعي دالة ونوعاً غير موجودين (`has_business_permission`/`business_permission`، سطر 41) فيفشل النشر، ولو طُبِّق لعطّل تريجره كل إدراج قيد مالي في النظام بمقارنة enum بقيم `'in'/'out'` غير الموجودة (الأسطر 51–111) — عطل شامل للوظيفة الجوهرية للمنتج (تقارير 01، 02، 03، 05، 06، 08، 09).
2. **عقد RPC بين الموبايل والباكند غير متطابق:** التطبيق يرسل معاملات وأسماء دوال غير موجودة (`create_ledger_entry` بستة معاملات زائدة، `command_resolve_dispute`، `command_generate_statement`...) فيفشل كل استدعاء بـ `PGRST202` وتبقى كل العمليات المالية في طابور الأوفلاين إلى الأبد بينما تعرض الواجهة أرصدة محلية وهمية (`merchant_repository.dart:129-143,349,367,422,452`؛ `sync_engine.dart:122-127`) — انحراف دائم وصامت بين أرصدة العميل وقاعدة البيانات (تقارير 02، 05، 06، 07، 08).
3. **مفتاح OpenWA API مسرّب وثابت في الكود:** قيمة حقيقية `owa_k1_...` مضمّنة في `send-whatsapp-otp/index.ts:4-5` ومكررة حرفياً في `whatsapp_otp_service.dart:28-29` — أي شخص يطّلع على المستودع أو يفكك APK/IPA يسيطر على جلسة واتساب التجارية بالكامل (تقارير 03، 04، 07، 08، 09، 10).
4. **مصادقة OTP والجلسات تُتحقق على جهاز العميل وقابلة للتجاوز كلياً:** توليد الرمز وتخزينه والتحقق منه محلياً (`whatsapp_otp_service.dart:95-99`)، مع كودي تجاوز ثابتين `'123456'/'000000'` (سطور 196-199)، وقبول `mockOtpCode` (`auth_controller.dart:397`)، وتسجيل دخول بديل بلا كلمة سر من الكاش المحلي (`auth_controller.dart:128-138`)، واستعادة جلسة من SharedPreferences بلا توكن (`auth_controller.dart:71-94`) — لا قيمة أمنية لإثبات الهوية إطلاقاً (تقارير 03، 04، 07، 08، 10).
5. **لا CI ولا اختبارات نفي أمنية ولا اختبارات موبايل:** لا يوجد أي pipeline، ولا اختبار RLS سلبي واحد (عزل المستأجرين جوهر المنتج)، ولا اختبار لأي من Edge Functions التسع، واختبار الموبايل الوحيد smoke test من 15 سطراً مقابل 53 ملف Dart تشمل أخطر منطق في المنتج (طابور الأوفلاين) — أي تراجع أمني أو كسر تكامل يمر دون اكتشاف (تقرير 10، البنود 1–3 و16).

### إحصائية الملاحظات الإجمالية (حسب التصنيف، كما أعلنتها التقارير)

| التقرير | حرج | عالي | متوسط | منخفض | إيجابي |
|---|---:|---:|---:|---:|---:|
| 01 المخطط والقيود | 5 | 5 | 6 | 6 | 10 |
| 02 الدوال والمنطق | 3 | 5 | 6 | 9 | 7 |
| 03 الأمان وRLS | 5 | 3 | 8 | 4 | 10 |
| 04 Edge Functions | 3 | 3 | 6 | 7 | 8 |
| 05 القيد المزدوج | 2 | 3 | 2 | 4 | 11 |
| 06 تعدد العملات | 5 | 5 | 4 | 3 | 7 |
| 07 طبقة بيانات الموبايل | 6 | 9 | 7 | 5 | 6 |
| 08 عقود API | 15 | 7 | 7 | 3 | 4 |
| 09 التوثيق مقابل التنفيذ | 3 | 4 | 10 | 4 | 7 |
| 10 الاختبارات والجاهزية | 5 | 7 | 6 | 2 | 5 |
| **الإجمالي** | **52** | **51** | **62** | **47** | **75** |

**المجموع الكلي: 287 ملاحظة** (212 ملاحظة سلبية + 75 نقطة إيجابية). ملاحظة منهجية: الأرقام معتمدة من إحصاءات كل تقرير كما أعلنها؛ الترقيم التفصيلي داخل تقرير 10 يصل إلى 9 بنود موسومة «عالي» بينما إحصاؤه المعلن 7 — اعتمدنا الإحصاء المعلن.

---

## 2. النتائج العابرة الحرجة (Cross-Cutting Critical Findings)

خمس مشاكل تكررت عبر عدة تقارير بأدلة مستقلة. نجمع هنا كل الأدلة من كل التقارير في مكان واحد.

### X-1: الترحيل 0012 (تعدد العملات والتصنيفات) مكسور بنيوياً على عدة مستويات

**ظهر في 7 تقارير من 10** (01، 02، 03، 05، 06، 08، 09). الأدلة المجمعة:

- **لن يُطبَّق أصلاً:** السياسة `customer_currency_balances_select_member` تستدعي `public.has_business_permission(business_id, 'view_balances'::public.business_permission)` وكلاهما غير معرّف في أي ملف بالمشروع — `202608180012_multi_currency_and_categories.sql:38-43` (01§1.1، 02§2، 03 ح-3، 05 حرج-1، 06 حرج-1، 08 C-6، 09 C-1). الملف أيضاً بلا غلاف `begin/commit` بخلاف 0001/0006/0010 (01§1.1، 02§2).
- **لو طُبِّق لعطّل كل الكتابة المالية:** التريجر `private.trig_update_customer_currency_balance` مربوط `after insert on public.ledger_entries` (0012:108-111) ويقارن `new.direction = 'in'` (سطر 59) بينما `public.ledger_direction` قيماه `('debit','credit')` فقط (`202607120001_core_schema.sql:20`) → خطأ `22P02 invalid input value for enum` عند كل إدراج قيد (01§1.2، 02§1، 03 ح-4، 05 حرج-1، 06 حرج-2).
- **مفتاح أجنبي للجدول الخاطئ:** `customer_currency_balances.customer_id references public.profiles(id)` (0012:17) بينما التريجر يدرج `new.customer_id` القادم من `ledger_entries.customer_id` الذي يشير إلى `public.customers(id)` (`202607120001_core_schema.sql:162`) → انتهاك FK شبه دائم (01§1.3، 02§1، 03 ح-4، 08 M-4، 09 C-1).
- **دالة عامة موازية بلا صلاحيات:** `public.command_create_ledger_entry` (0012:114-209) بلا أي فحص `is_business_member`، `security definer` بلا `set search_path=''`، بلا revoke (منح PUBLIC الافتراضي)، تستخدم `'fee'` غير الموجودة في `ledger_entry_type` (سطر 157)، `p_source_device_id text` في عمود `uuid` (سطر 128 مقابل 0001:173)، و`p_description default ''` يخالف قيد الطول (سطر 123 مقابل 0001:167) (01§1.4، 02§3، 03 ح-2، 05 حرج-1، 06 حرج-3، 09 C-1).
- **تناقض بنيوي مع 0010:** القيد `ledger_entries_check` المعدَّل في `202608140010:314-319` يشترط `debit/credit` بينما 0012 يدرج `'in'/'out'` — الترحيلان لا يمكن أن يعملا معاً (01§1.5، 06 حرج-3).
- **تناقض معماري مع حارس العملة الواحدة:** `validate_ledger_entry_insert` (0010:329-330) يفرض أن عملة كل قيد = عملة المحل، فجدول الأرصدة متعدد العملات لن يحمل إلا عملة واحدة عملياً (01§2.1، 02#6، 06 حرج-5، 08 C-7).
- **خارج كل مراجع الحقيقة:** غير موثق في أي وثيقة، و`full_schema.sql` (3070 سطراً) يتوقف عند 0011، ولا اختبار يغطيه (01§3.1، 06 منخفض-15، 09 C-1/L-4، 10 #4).

**الحكم المجمع:** يبدو أن 0012 كُتب ضد نسخة مخطط مختلفة تماماً لم تصل إلى هذا المستودع (01§1.5). لا يجوز دمجه بحالته الحالية؛ إما حذفه أو إعادة كتابته من الصفر بعد حسم القرار المعماري لتعدد العملات.

### X-2: مفتاح OpenWA مسرّب في الكود وفي حزمة التطبيق

**ظهر في 6 تقارير** (03، 04، 07، 08، 09، 10). الأدلة المجمعة:

- `supabase/functions/send-whatsapp-otp/index.ts:4-5`: كانت توجد قيم افتراضية حقيقية لـ OpenWA وتم حجبها (03 ح-1، 04 ح-1، 09 C-3، 10 #18).
- **نفس المفتاح مكرر حرفياً في الموبايل:** `mobile/lib/core/services/whatsapp_otp_service.dart:28-29` كثوابت `const` — يُستخرج من أي APK/IPA (03 ح-1، 04 ح-1، 07 ح-6، 08 C-10).
- عناوين شبكة داخلية ثابتة للتطوير في `whatsapp_otp_service.dart:32-36` (`http://192.168.0.134:2785/api`، localhost، 10.0.2.2) (03 ح-1، 07 ح-6، 08 C-10).
- الدالة نفسها بلا مصادقة وبلا rate limit وCORS مفتوح `"*"` (`send-whatsapp-otp/index.ts:13-79`)، وغير مدرجة في `config.toml` `[functions.*]` — أي مجهول يرسل رسائل واتساب باسم النشاط (03 ع-1، 04 ح-2، 09 C-3).
- تناقض توثيقي: الدالة غير موثقة في أي قائمة، وتخالف النطاق الموثق صراحة («WhatsApp/SMS جماعي مدفوع» خارج النطاق — `first-version-product-architecture-spec.md:65`) (09 C-3).

**الإجراء المجمع:** إبطال المفتاح فوراً، إزالة كل القيم الافتراضية، نقله إلى أسرار Edge Function فقط، منع اتصال العميل ببوابة واتساب مباشرة، وإضافة فحص أسرار (gitleaks/trufflehog) إلى CI (10 توصية 5).

### X-3: عدم تطابق عقد RPC بين الموبايل والباكند

**ظهر في 5 تقارير** (02، 05، 06، 07، 08). الأدلة المجمعة:

- **`create_ledger_entry`:** الموبايل يرسل 12 معاملاً تشمل `p_currency_code, p_category, p_payment_method, p_reference_number, p_bank_or_agent_name, p_attachment_path` (`merchant_repository.dart:129-143`، يُرسل عبر `sync_engine.dart:123`) بينما `public.create_ledger_entry` تقبل 9 معاملات فقط بلا أي منها (`202607120002:413-425`) → `PGRST202` دائم (05 حرج-2، 06 حرج-4، 07 ح-1، 08 C-2).
- **`apply_customer_discount`:** الموبايل يرسل `p_currency_code` (`merchant_repository.dart:190-196`) غير الموجود في التوقيع (`202608140010:404-408`) (05 حرج-2، 06 حرج-4، 07 ح-1، 08 C-4).
- **`reverse_ledger_entry`:** الموبايل يرسل `p_original_entry_id` (`merchant_repository.dart:259-263`) والدالة تتوقع `p_entry_id` (`202607120002:453`) (07 ح-1، 08 C-3).
- **أربع دوال بأسماء غير موجودة إطلاقاً:** `command_add_dispute_message` (سطر 349)، `command_resolve_dispute` (367، مع `p_note` بدل `p_resolution_note`)، `command_invite_business_member` (422، مع `p_phone` بدل `p_target_user_id`)، `command_generate_statement` (452) — الصحيح: `add_dispute_message` (0002:563)، `resolve_dispute` (0002:606)، `invite_business_member` (0008:60)، `create_statement` (0008:185) (02#23، 07 ح-2، 08 C-1).
- **قيمة scope خاطئة:** `p_scope: 'customer'` (`merchant_repository.dart:453`) بينما enum `statement_scope` قيمتاه `'business_customer'/'customer_consolidated'` (0001:31) (08 H-2).
- **استعلامات مباشرة مكسورة:** `getMembers` يقرأ أعمدة `member_role, permissions, is_active` غير الموجودة (الفعلية `role, status` — `merchant_repository.dart:385-391`)؛ `getDisputes` يضمّن `profiles!customer_id` بلا FK (295-300)؛ `getDisputeMessages` يضمّن `profiles` بلا FK (333-336)؛ `profiles` يُقرأ/يُكتب بعمودي `user_type/phone` غير الموجودين (`auth_controller.dart:118-119,230-235,416-421`) (08 C-11/C-12/C-13/C-14).
- **الأثر المركّب:** كل عملية مالية أوفلاين تبقى في `offline_mutations_queue` للأبد تُعاد كل 30 ثانية وتفشل بصمت (الخطأ في `debugPrint` فقط — `sync_engine.dart:137`)، والواجهة تعرض أرصدة محلية كأنها موثقة (07 ح-1)؛ والنزاعات والدعوات والكشوفات معطلة 100% مع ابتلاع الاستثناءات وإرجاع `false/null` بصمت (07 ح-2، 08 C-1).

### X-4: مصادقة OTP والجلسات على جهاز العميل — لا قيمة أمنية

**ظهر في 5 تقارير** (03، 04، 07، 08، 10). الأدلة المجمعة:

- توليد الرمز على الجهاز (`whatsapp_otp_service.dart:95-99`)، التخزين في ذاكرة التطبيق `_activeOtps` (سطر 39)، والتحقق محلياً (سطور 188-241) (03 ح-5، 04 ح-2).
- كودا تجاوز ثابتان `'123456'` و`'000000'` يُقبلان دائماً (`whatsapp_otp_service.dart:196-199`)، والرمز الافتراضي `'123456'` معرّف في `AuthState` نفسه (`auth_controller.dart:36`) (07 ح-3، 08 C-10).
- قبول `state.mockOtpCode` عند فشل الخدمة — والرمز «السري» مخزن أصلاً في حالة التطبيق (`auth_controller.dart:397`، و315-324) (07 ح-3، 08 C-10).
- **تسجيل دخول بديل بلا بيانات اعتماد:** عند فشل `signInWithPassword` لأي سبب يُقبل الدخول من الكاش المحلي بلا مقارنة كلمة سر (`auth_controller.dart:128-138`)، و`registerWithPassword` ينشئ حساباً محلياً وهمياً عند فشل السيرفر (238-289) (07 ح-4، 08 C-9).
- **استعادة جلسة بلا توكن:** وجود `cached_user_id` في SharedPreferences يكفي لاعتبار المستخدم `authenticated` (`auth_controller.dart:71-94`) — انتحال هوية trivial على جهاز rooted (07 ح-5).
- لا يوجد تحقق هاتف بديل من مزوّد: `config.toml:59-61` يعطّل SMS OTP، و`:55` يعطّل التسجيل بالبريد (03 ح-5، 10 #19).

### X-5: غياب CI والاختبارات — لا بوابة جودة إطلاقاً

**ظهر في 4 تقارير** (05، 06، 09، 10). الأدلة المجمعة:

- **لا CI إطلاقاً:** لا `.github/workflows` ولا أي pipeline في المشروعين (بحث شامل عن `*.yml`) — التشغيل محلي فقط موثق في `README.md:44-49` (10 #16).
- **31 تأكيد pgTAP فقط** في 3 ملفات، كلها مسار سعيد: لا اختبار RLS سلبي واحد رغم أن `README.md:94-103` يسردها كموصى بها (10 #1)؛ لا اختبار لأي Edge Function (10 #2)؛ اختبار موبايل وحيد smoke من 15 سطراً مقابل 53 ملف Dart (10 #3).
- **فجوات محاسبية:** لا اختبار للعكس (reversal)، ولا لرفض القيد غير المتوازن، ولا لقفل الفترات، ولا لعدم القابلية للتعديل (05#5، 10 #6).
- **تعدد العملات بلا أي اختبار** — البحث عن `currency` في مجلد الاختبارات بلا نتائج (06 توصية 6، 10 #4).
- **لا اختبارات تزامن** رغم أن `First-version.md:1536` يطلبها صراحة (10 #5).
- **الترحيل 0012 خارج كل مراجع الحقيقة:** وثائق/مخطط مجمع/اختبارات (09 L-4).

---

## 3. أقسام المجالات العشرة (خلاصة كل تقرير)

### 3.1 المخطط والقيود (Schema & Constraints) — التقرير 01

**الخلاصة:** المخطط الأساسي (0001–0011) متماسك ومهني: مبالغ `numeric(20,4)` بلا فاصلة عائمة، `timestamptz` في كل مكان، سجل مالي append-only، قيود CHECK شاملة، وفهارس جيدة. الخلل كله في الترحيل الأخير 0012 المكسور بنيوياً (تفاصيله في X-1 أعلاه)، إضافة إلى أن `full_schema.sql` متوقف عند 0011.

**أهم الملاحظات (مرتبة حسب الخطورة):**
- [حرج] 0012 لن يُطبَّق (دالة/نوع غير موجودين، 0012:38-43)؛ تريجر يعطّل كل إدراج قيد (0012:51-111)؛ FK للجدول الخاطئ (0012:17)؛ دالة عامة بلا صلاحيات (0012:114-209)؛ تناقض مع قيد 0010 (0010:314-319).
- [عالي] تضارب مفاهيمي 0010/0012 حول تعدد العملات؛ حذف CASCADE في جدول مالي يكسر سياسة RESTRICT (0012:15-16)؛ حالة `voided` غير قابلة للوصول (0010:10,121-124)؛ رصيد مشتق بلا مصالحة ولا حماية (0012:13-26)؛ سياسة service_role ميتة توحي بحماية زائفة (0012:45-48).
- [متوسط] `full_schema.sql` غير محدث؛ `category/payment_method` نص حر بلا قيود (0012:5-10)؛ `attachment_url` مسار مرفقات موازٍ (0012:10)؛ تحويل صامت للعملة غير الصالحة إلى 'YER' (0012:147-150)؛ `sync_checkpoints.updated_at` بلا تريجر (0006:36)؛ قيود اليومية بلا عملة وفترات مفتوحة متداخلة ممكنة (0010:76-88,55).

**التقرير التفصيلي:** `analysis-reports/01-schema-constraints.md` (حرج 5 · عالي 5 · متوسط 6 · منخفض 6 · إيجابي 10).

### 3.2 الدوال والمشغلات وتسلسل العمليات — التقرير 02

**الخلاصة:** البنية الأساسية متينة (نمط أوامر `private.command_*` بأغلفة عامة رفيعة، idempotency صحيح، outbox تعاملي احترافي)، لكن 0012 كتلة مكسورة بالكامل، وتوجد تسابقات حقيقية وفجوات idempotency. ملاحظة منهجية: `private.command_create_ledger_entry` أُعيد تعريفها 4 مرات (0002←0006←0007←0010) والتحليل يستند للنسخة النهائية.

**أهم الملاحظات:**
- [حرج] ثلاثيتها كلها داخل 0012 (انظر X-1).
- [عالي] **تسابق العكس مقابل الإنشاء:** `command_create_ledger_entry` يقفل صف `business_customers` (0006:267) بينما `command_reverse_ledger_entry` يقفل صف القيد فقط (0006:314) — سداد + عكس متزامنان يدفعان الرصيد إلى −100 رغم نجاح كل فحص على حدة؛ والعكس بلا فحص رصيد أصلاً. **idempotency معلن لثمانية أوامر ومنفذ لثلاثة** (0011:5-10)؛ `command_post_manual_journal` لا يستقبل `client_request_id` أصلاً (0010:410-436). **كشف الحساب ليس لقطة متسقة** — ثلاثة استعلامات بلا قفل (0008:152-180) وبلا receipt. **دوال أُنشئت بعد 0003 تحتفظ بمنح PUBLIC الافتراضي** (0003:33).
- [متوسط] حارس «السداد يتجاوز الرصيد» معطّل افتراضياً (`allow_customer_credit_balance default true`، 0010:39)؛ قبول نزاع جزئي على خصم يفشل دائماً (0006:463-468 + 0010:356)؛ عكس قيد عليه نزاع نشط يعلّق النزاع (0006:302-336)؛ فجوات سجل التدقيق (لا تدقيق على `accounting_periods` و`chart_of_accounts` — 0005:37-43)؛ تسابق تأكيد/نزاع وتأكيد قيود معكوسة (0006:339-387).

**التقرير التفصيلي:** `analysis-reports/02-functions-logic.md` (حرج 3 · عالي 5 · متوسط 6 · منخفض 9 · إيجابي 7).

### 3.3 الأمان وRLS — التقرير 03

**الخلاصة:** النموذج العام default-deny سليم البنية: RLS مفعّل على كل الجداول العامة الـ 31 بلا استثناء، سحب شامل ثم منح انتقائية (0003:30-53)، عزل PII بتشفير AES-GCM، وكل الـ views بـ `security_invoker=true`. لكن ثغرات 0012 وطبقة OTP/واتساب تُبطل جزءاً من هذا الانضباط.

**أهم الملاحظات:**
- [حرج] مفتاح OpenWA مكشوف (X-2)؛ دالة 0012 العامة بلا تفويض (X-1)؛ سياسة 0012 غير القابلة للتطبيق (X-1)؛ تضارب FK/enum في 0012 (X-1)؛ التحقق من OTP على العميل (X-4).
- [عالي] `send-whatsapp-otp` إرسال اعتباطي بلا مصادقة (X-2)؛ **رفع المستندات يتحقق من القراءة فقط ثم يكتب بـ service_role** — العميل read-only ودور viewer يستطيعان إرفاق ملفات بأي قيد يقرأانه (`signed-document-upload/index.ts:10-14`، `finalize-document-upload/index.ts:9-13`)، تعارض صريح مع `docs/rls-matrix.md:8-14`؛ **باقة `business-assets` عامة وتسمح بـ SVG** — XSS مخزّن محتمل (0004:19).
- [متوسط] دوال 0012 بلا `set search_path`؛ دوال عامة جديدة بلا revoke من PUBLIC/anon؛ منح EXECUTE على `private.command_*` لـ authenticated يتجاوز طبقة الـ wrapper (0003:68-83)؛ تعارض صلاحيات cashier مع المصفوفة الموثقة (0003:132-134 مقابل `rls-matrix.md:22`)؛ سياسات قديمة لا تتحقق من `link_status` (0003:147-152)؛ `upload_sessions` بلا revoke ولا policy (0006:79-93,522-523)؛ سياسة `customer_currency_balances` تقارن هوية خاطئة (0012:42)؛ بقايا تطوير في `config.toml:48,50-61`.

**التقرير التفصيلي:** `analysis-reports/03-security-rls.md` (حرج 5 · عالي 3 · متوسط 8 · منخفض 4 · إيجابي 10).

### 3.4 دوال Edge التسع — التقرير 04

**الخلاصة:** البنية المشتركة `_shared` رصينة (تحقق JWT فعلي، فصل صلاحيات least-privilege، تشفير هواتف صحيح، rate limiting ذرّي مركزي، معالج طابور متين). الكسور تتركز في `send-whatsapp-otp` (أسرار + بلا حماية) وIDOR في `generate-statement` وأخطاء منطقية في عمال الخلفية.

**أهم الملاحظات:**
- [حرج] أسرار مرمّزة في الكود (X-2)؛ `send-whatsapp-otp` بلا مصادقة وكسر نموذج OTP (X-2/X-4)؛ **IDOR في `generate-statement`:** عند تمرير `statementId` جاهز يُجلب الكشف عبر service client بلا أي تحقق ملكية (سطور 425، 449-457) ويُعاد مع الأرصدة ورمز التحقق ورابط PDF موقّع (534-552) — كشف كامل للبيانات المالية لأي مستخدم مصادق.
- [عالي] تجاوز rate limiting في `verify-statement` عبر ترويسة `x-forwarded-for` مزوّرة (سطور 13-14) مع رمز تحقق 48 بت قابل للتعداد؛ `p_key_version` ثابتة =1 في `customer-directory:35` تكسر تدوير مفاتيح التشفير؛ تكرار الإشعارات عند فشل توكن جزئي في `process-notification-outbox:169-216`.
- [متوسط] حساب تواريخ الأتمتة بـ UTC بدل timezone النشاط — انزياح يوم كامل في التذكيرات (`process-automation-rules:83,114-120,143`)؛ نافذة الإرسال العابرة لمنتصف الليل لا تعمل إطلاقاً (56-58)؛ تخويل الرفع على صلاحية القراءة (مشترك مع 03)؛ لا فحص magic bytes للملفات (`finalize-document-upload:30-33`)؛ عمود الرصيد الجاري في PDF يفقد الإشارة السالبة بـ `Math.abs` (`generate-statement:45-51,337`)؛ مدخلات مالية/زمنية بلا تحقق نطاق.

**التقرير التفصيلي:** `analysis-reports/04-edge-functions.md` (حرج 3 · عالي 3 · متوسط 6 · منخفض 7 · إيجابي 8).

### 3.5 المحاسبة بالقيد المزدوج — التقرير 05

**الخلاصة:** النواة المحاسبية (009–011) **سليمة رياضياً ومحاسبياً وتستحق الثقة**: تحقق يدوي من كل ترحيل (افتتاحي/دين/سداد/خصم/عكس) — كلها متوازنة وصحيحة، والتوازن مفروض بـ constraint trigger مؤجل على مستوى قاعدة البيانات (0010:204-221)، والذرّية كاملة (مستحيل قيد تشغيلي بلا يومية). لكن هذا البناء محاصر بـ 0012 وعقد API المكسورين وفجوة اختبارات.

**أهم الملاحظات:**
- [حرج] 0012 مكسور ويهدد سلامة المحاسبة (X-1) — ومحاسبياً يُدخل تعدد عملات دون أي تعديل على طبقة القيد المزدوج: `journal_entry_lines` بلا عمود عملة وميزان المراجعة سيجمع عملات مختلفة في حساب واحد؛ عقد API غير المتطابق يمنع وصول بيانات التطبيق أصلاً (X-3).
- [عالي] قبول النزاع جزئياً على قيد خصم يفشل دائماً (0002:586-589 + 0010:356)؛ وصف القيد اليومي قد يتجاوز 500 حرف ويسقط العملية كلها (0010:260 — بادئة تصل 30 حرفاً + وصف حتى 500 مقابل حد 500 في 0010:68)؛ تغطية الاختبارات مسار سعيد فقط — العكس وقفل الفترة والرفض السلبي بلا اختبار.
- [متوسط] مصدران للحقيقة في رصيد العميل بلا آلية تسوية (0010:479-490)؛ إيصالات الأوامر غير مكتملة للقيود اليدوية والعكسيات (0011:5-10).

**التقرير التفصيلي:** `analysis-reports/05-double-entry.md` (حرج 2 · عالي 3 · متوسط 2 · منخفض 4 · إيجابي 11).

### 3.6 تعدد العملات — التقرير 06

**الخلاصة:** الميزة **غير قابلة للتشغيل إطلاقاً بحالتها الحالية** — «حرجة بامتياز». الـ migration لن ينجح، والتريجر والدالة يستخدمان قيماً غير موجودة في الـ enums، والموبايل يستدعي تواقيع لا تطابق أي دالة منشورة، وتريجر التحقق الأصلي يمنع أي عملة مخالفة لعملة المحل. لا يوجد أي تحويل بين العملات (لا أسعار صرف)، والتصميم يفترض فصلاً كاملاً بين العملات لكن الشاشات والـ views والمحاسبة تجمع المبالغ بلا تمييز عملة في مواضع عدة.

**أهم الملاحظات:**
- [حرج] كلها في X-1 وX-3 (الـ migration غير قابل للتطبيق، تريجر enum، دالة فاشلة منطقياً، عدم تطابق تواقيع RPC، التناقض المعماري مع حارس العملة الواحدة 0010:329-330).
- [عالي] **الـ views ودوال الكشوفات تجمع عملات مختلفة بلا تجميع:** `business_customer_balances` (0002:653-670) و`business_customer_account_positions` (0010:479-490) و`command_generate_statement` (0008:152-164) — «500 ر.ي + 100 $ = 600 ر.ي» في كشوفات موقعة قانونياً؛ `journal_entry_lines` بلا عملة (0010:76-88)؛ أرصدة العملات المحلية في الموبايل لا تُزامَن من السيرفر إطلاقاً والتحديث التفاؤلي يتجاهل `reversal/opening_balance` (`sync_engine.dart:187-193`، `app_database.dart:405-411`)؛ إجماليات لوحة التاجر تجمع عملات مختلفة معروضة برمز واحد (`merchant_controller.dart:122-124`، `merchant_home_screen.dart:447-456`).
- [متوسط] رموز `'ر.ي'` ثابتة في شاشات تعرض عملات أخرى (5 مواقع)؛ لا أسعار صرف وعملة المحل مثبتة `'YER'` برمجياً في 4 مواضع (`auth_controller.dart:164,262,453`، `merchant_controller.dart:98`)؛ التصنيفات نصوص حرة بلا قيود؛ إشعارات الأتمتة تستخدم عملة المحل بدل عملة القيد (`process-automation-rules:207,235`).

**التقرير التفصيلي:** `analysis-reports/06-multi-currency.md` (حرج 5 · عالي 5 · متوسط 4 · منخفض 3 · إيجابي 7).

### 3.7 طبقة بيانات الموبايل (Flutter) — التقرير 07

**الخلاصة:** البنية المعمارية (كتابة تفاؤلية + طابور أوامر + RPC فقط) صحيحة نظرياً، لكن التنفيذ فيه **كسر كامل في عقد الاتصال** (X-3) ومسارات التفاف كاملة في المصادقة (X-4). النتيجة: انحراف دائم وصامت بين أرصدة العميل المعروضة وقاعدة البيانات المركزية — «أسوأ سيناريو ممكن لمنتج مالي».

**أهم الملاحظات:**
- [حرج] عدم تطابق RPC (X-3)؛ أربع عمليات تستدعي دوالاً غير موجودة (X-3)؛ OTP على العميل مع أكواد التفاف (X-4)؛ تسجيل دخول بلا بيانات اعتماد (X-4)؛ استعادة جلسة بلا توكن (X-4)؛ مفاتيح وأسرار مضمّنة في كود العميل (X-2 + `supabase_config.dart:8-13` عنوان `127.0.0.1:55321` ومفتاح anon محلي افتراضي، وعنوان Supabase قابل للتعديل من المستخدم عبر SharedPreferences بلا تحقق — سطور 36-49).
- [عالي] حساب الأرصدة في العميل يتجاهل `reversal` — رصيد خاطئ فوراً بعد أي عكس (`app_database.dart:405-411`)؛ **إضافة عميل جديد لا تُدرج أي أمر في طابور المزامنة** — العميل لا يصل للسيرفر أبداً وكل قيوده ميتة (`merchant_repository.dart:46-81`)؛ السحب لا يجلب `ledger_entries` إطلاقاً ويكتب فوقياً بلا حل تعارضات (`sync_engine.dart:147-220`)؛ كتابة مباشرة على `profiles` (خرق API-only — `auth_controller.dart:230,416`)؛ **حذف طابور العمليات غير المرسلة عند تسجيل الخروج** — فقدان بيانات مالية بصمت (`auth_controller.dart:505` + `app_database.dart:511-520`)؛ لا حد أقصى لإعادة المحاولة ولا dead-letter؛ `occurred_at` من ساعة الجهاز القابلة للتعديل؛ التخزين المحلي غير مشفر (SQLite بلا SQLCipher + `pendingPassword` نص صريح في الذاكرة)؛ معرّفات محلية `'entry-/cust-/biz-'` لا تُسوَّى مع معرّفات السيرفر.
- [متوسط] N+1 في سحب الأرصدة؛ عملة العكس الافتراضية 'YER' خاطئة (`merchant_repository.dart:232`)؛ ابتلاع أخطاء وإرجاع بيانات قديمة كأنها حية؛ ازدواجية مصادر OTP؛ `offlineMockMode` قابل للتفعيل من المستخدم (`supabase_config.dart:17,29,39-48`)؛ مسارات أوامر ميتة في المزامنة؛ ترحيل SQLite هش بـ try/catch صامتة.

**التقرير التفصيلي:** `analysis-reports/07-mobile-data-layer.md` (حرج 6 · عالي 9 · متوسط 7 · منخفض 5 · إيجابي 6).

### 3.8 عقود API بين الواجهات والباكند — التقرير 08

**الخلاصة:** الفجوة **جذرية وليست هامشية** — «التطبيق أقرب إلى نموذج عرض (demo) يعمل على SQLite محلي؛ التكامل الفعلي مع الباكند شبه معدوم». 15 ملاحظة حرجة توثق أن غالبية العمليات غير موصولة: مصفوفة التطابق النهائي (§9 في التقرير) تُظهر 20 عملية، منها 17 بحكم ❌ (غير موصول/معطل) و2 ⚪ (ميزات بلا واجهة مقبولة للـ MVP) وواحدة فقط سليمة الاتجاه.

**أهم الملاحظات (إضافة لما في X-2/X-3/X-4):**
- [حرج] **شاشة العميل بأكملها بيانات وهمية ثابتة** — رصيد ثابت `'450,000 ر.ي'` وزر «تأكيد السجل» يعرض نجاحاً بلا أي استدعاء (`customer_home_screen.dart:94,181-195,264-272`) — كل تجربة العميل (تأكيد القيود، الرد على الربط، النزاعات، الكشف المجمع) غير منفذة؛ `profiles` بأعمدة وهمية وبلا منحة INSERT (C-11)؛ `getMembers`/`getDisputes`/`getDisputeMessages` باستعلامات مكسورة (C-12/13/14)؛ **المرفقات لا تُرفع إلى Storage إطلاقاً** — «سند موثق بالمرفقات» غير موجود فعلياً (C-15).
- [عالي] **صفر استدعاءات لكل Edge Functions التسع** (`functions.invoke` غير موجودة في كل `mobile/lib`) — لا PDF، لا تشفير هاتف، لا رفع مرفقات؛ تجاهل بنية الأوفلاين الخلفية (`command_receipts`/`sync_checkpoints`/`device_installations` من 0006)؛ مسارات ميتة في محرك المزامنة (`confirm_ledger_entry`/`open_dispute` لا يُدرجهما أحد في الطابور)؛ عمليات النزاع أونلاين فقط وتفشل بصمت.
- [متوسط] `full_schema.sql` بتعريفات مكررة متضاربة؛ `phone` غير موجود في `business_customers`؛ نوع `'fee'` الشبح؛ N+1؛ `sync_status` لا يُحدَّث بعد النجاح؛ سقوط `loginWithPassword` للكاش الوهمي حتى عند نجاح الدخول الفعلي (بسبب عمود `user_type` الوهمي).

**التقرير التفصيلي:** `analysis-reports/08-api-contracts.md` (حرج 15 · عالي 7 · متوسط 7 · منخفض 3 · إيجابي 4).

### 3.9 التوثيق مقابل التنفيذ — التقرير 09

**الخلاصة:** 28 ملاحظة (3 حرجة) تكشف انفصالاً بين الوثائق والواقع في الاتجاهين: ميزات منفذة توصف كغير موجودة، وميزات موصوفة كمنفذة وهي مكسورة، وترحيل كامل (0012) خارج كل الوثائق. المطابقات الإيجابية واسعة أيضاً: كل RPCs الموثقة في مرجع API منفذة بنفس الأسماء والمعاملات والأدوار (P-1)، والمبادئ المالية غير القابلة للتنازل منفذة كما وُثقت (P-2).

**أهم الملاحظات:**
- [حرج] 0012 غير موثق ومكسور (X-1)؛ **`process-automation-rules` موثق كعامل جاهز لكنه مكسور وقت التشغيل في مساريه:** إدراج في `reminders` بأعمدة غير موجودة (`ledger_entry_id/title/body` — الجدول الفعلي يتطلب `sent_by_user_id` و`message_snapshot`، 0001:326-338)، استعلام `disputes.status` غير الموجود (الحالة في `dispute_state`)، وإشعارات تُدرج مباشرة متجاوزة `enqueue_notification` فلا يصل أي Push إطلاقاً (`index.ts:213-221,274,225-237`)؛ `send-whatsapp-otp` غير موثقة بأسرار مضمّنة وبلا حماية (X-2).
- [عالي] وثيقتان مرجعيتان تدّعيان أن `generate-statement` يعيد 501 غير منفذ بينما هو منفذ بالكامل (562 سطراً) — قرار إطلاق مبني عليهما خاطئ (`api-reference-and-operational-flow.md:127`، `database-architecture-and-operations-spec.md:21,290`)؛ الوثائق تدّعي `process-automation-rules` معطّل بينما هو «مفعّل ظاهرياً وفاشل فعلياً» — أخطر من المعطّل؛ **PDF كشف الحساب إنجليزي فقط بخط Helvetica لا يدعم العربية** — وصف القيد العربي سيظهر مشوهاً في وثيقة مالية موقعة، مخالفاً وعد «العربية أولاً» (`generate-statement:65-66,86,235-241`)؛ `cleanup-expired-data` موثقة وغير موجودة (مع `upload_sessions` المنتهية بلا منظّف).
- [متوسط] جداول موثقة كمطلوبة وغير منفذة (`dispute_sla`/`security_events`/`business_settings`)؛ 0012 يخالف مبدأ «الرصيد يُشتق ولا يُخزن» وقرار «عملة واحدة لكل محل» الموثقين؛ تناقضات داخلية في الوثائق (حدود الترحيلات، أسماء الملفات، قيمة `discount` الناقصة، سياسة الدفع الأكبر من الرصيد)؛ `First-version.md` يقترح NestJS/Laravel بينما التنفيذ Supabase؛ `edge-functions.md` يسرد 6 من 9؛ قوائم RPC ناقصة/زائدة بين الوثائق.

**التقرير التفصيلي:** `analysis-reports/09-docs-vs-implementation.md` (حرج 3 · عالي 4 · متوسط 10 · منخفض 4 · إيجابي 7).

### 3.10 الاختبارات وجاهزية الإطلاق — التقرير 10

**الخلاصة:** 31 تأكيد pgTAP جيدة الصنع (حتمية، داخل معاملة بـ rollback، تحاكي مستخدمين حقيقيين تحت RLS) تغطي المسار السعيد بشكل جيد — لكن لا CI، لا اختبارات نفي، لا اختبارات موبايل حقيقية، سر مرتكب في الكود، مسار مصادقة إنتاجي غير محسوم، ولا نسخاً احتياطياً أو مراقبة. تقدير التقرير: جاهزية «تجريبي مغلق» تحتاج **2–3 أسابيع عمل مركّز** على البنود 1–8 من قائمة الفجوات.

**أهم الملاحظات:**
- [حرج] لا اختبارات RLS سلبية إطلاقاً (`001_schema_contract.sql:11-13` يتحقق من *وجود* السياسات لا فعاليتها)؛ لا اختبارات للـ Edge Functions التسع؛ اختبارات موبايل شبه معدومة (`widget_test.dart` من 15 سطراً — أخطر منطق، طابور الأوفلاين، بلا اختبار)؛ لا CI إطلاقاً؛ سر OpenWA مثبت في الكود (X-2).
- [عالي] تعدد العملات غير مختبر؛ لا اختبارات تزامن؛ عدم القابلية للتعديل غير مختبرة مباشرة؛ **تناقض سياسة الدفع الأكبر من الرصيد** — `First-version.md:1037-1043` يقرر المنع بينما التنفيذ يسمح رصيداً دائناً (`0010:39` + الاختبار `003:33` يختبر دفعة 230 مقابل رصيد 180 كسلفة)؛ سيناريوهات النزاعات ناقصة؛ صلاحيات الأدوار غير مختبرة سلبياً؛ مسار OTP الإنتاجي غير محسوم (`config.toml:55,59-61`)؛ لا استراتيجية نسخ احتياطي/استعادة موثقة رغم كونها ميزة معلنة (`First-version.md:102,1380`)؛ لا مراقبة إطلاقاً (لا Sentry/Crashlytics في `pubspec.yaml`).
- [متوسط] قيود المبالغ غير مختبرة؛ لا اختبار لـ `verify-statement` العام؛ `seed.sql` فارغ عملياً (سيعيق المرحلة التجريبية المغلقة)؛ لا اختبارات أتمتة/إشعارات ولا أداء؛ `mobile/README.md` ما زال قالب Flutter الافتراضي.

**التقرير التفصيلي:** `analysis-reports/10-tests-launch-readiness.md` (حرج 5 · عالي 7 · متوسط 6 · منخفض 2 · إيجابي 5).

---

## 4. مصفوفة المخاطر الموحدة (كل الملاحظات الحرجة والعالية)

الملاحظات المكررة عبر عدة تقارير دُمجت في صف واحد مع تجميع كل مصادرها. عمود «المصدر» يشير إلى رقم التقرير والملف:السطر كما ورد فيه. الجهد تقديري (س = ساعات، ي = يوم عمل).

### 4.1 الملاحظات الحرجة (26 بنداً موحداً من 52 ملاحظة أصلية)

| # | الوصف | المصدر (تقرير — ملف:سطر) | الجهد |
|---|---|---|---|
| K1 | الترحيل 0012 غير قابل للتطبيق: سياسة RLS تستدعي `has_business_permission`/`business_permission` غير الموجودين + بلا `begin/commit` | 01/02/03/05/06/08/09 — `202608180012:38-43` | 1–2 ي (ضمن إعادة كتابة 0012) |
| K2 | تريجر 0012 يقارن enum بـ `'in'/'out'` → تعطيل كل إدراج قيد مالي في النظام | 01/02/03/05/06/08/09 — `0012:51-111` (خاصة 59) | س (ضمن K1) |
| K3 | FK خاطئ: `customer_currency_balances.customer_id → profiles(id)` بدل `customers(id)` | 01/02/03/05/08/09 — `0012:17` مقابل `0001:162` | س (ضمن K1) |
| K4 | دالة عامة `public.command_create_ledger_entry` بلا فحص تفويض وبأخطاء نوع متعددة — كتابة عابرة للمستأجرين ومسار موازٍ بلا ضوابط | 01/02/03/05/06/09 — `0012:114-209` | س–1 ي (ضمن K1) |
| K5 | تناقض بنيوي 0010↔0012: قيد CHECK يفرض `debit/credit` والدالة تدرج `'in'/'out'` و`'fee'` | 01/02/03/06/09 — `0010:314-319` مقابل `0012:157-163` | س (ضمن K1) |
| K6 | مفتاح OpenWA وجلسة مضمّنان نصياً في Edge Function ومكرران في كود الموبايل (APK/IPA) | 03/04/07/08/09/10 — `send-whatsapp-otp/index.ts:4-5`؛ `whatsapp_otp_service.dart:28-29` | س (تدوير فوري) + س (تنظيف الكود) |
| K7 | OTP يُولَّد ويُخزَّن ويُتحقق على العميل + كودا تجاوز ثابتان `123456/000000` + قبول `mockOtpCode` | 03/04/07/08 — `whatsapp_otp_service.dart:95-99,188-241,196-199`؛ `auth_controller.dart:36,315-324,397` | 2–3 ي (نقل server-side) |
| K8 | `send-whatsapp-otp` بلا مصادقة ولا rate limit وCORS `*` — إرسال واتساب اعتباطي باسم النشاط | 03/04/09/10 — `send-whatsapp-otp/index.ts:13-79` | 1 ي |
| K9 | IDOR في `generate-statement`: جلب أي كشف بمعرّفه عبر service client بلا تحقق ملكية + إرجاع رابط PDF موقّع | 04 — `generate-statement/index.ts:425,449-457,534-552` | س |
| K10 | حمولة `create_ledger_entry` من الموبايل بستة معاملات غير موجودة في التوقيع → فشل `PGRST202` دائم لكل قيد | 02/05/06/07/08 — `merchant_repository.dart:129-143`؛ `sync_engine.dart:123`؛ `0002:413-425` | 2–3 ي (توحيد العقد) |
| K11 | أربع عمليات تستدعي RPCs بأسماء غير موجودة (`command_add_dispute_message/resolve_dispute/invite_business_member/generate_statement`) + معاملات خاطئة | 02/07/08 — `merchant_repository.dart:349,367,422,452` | 1 ي |
| K12 | `reverse_ledger_entry` بمعامل `p_original_entry_id` بدل `p_entry_id`، و`apply_customer_discount` بمعامل زائد `p_currency_code` | 06/07/08 — `merchant_repository.dart:190-196,259-263` | س |
| K13 | إضافة عميل جديد لا تصل الباكند إطلاقاً (لا RPC ولا إدراج في الطابور) → سلسلة كسر: عميل → قيود → أرصدة → كشوفات | 07/08 — `merchant_repository.dart:46-81` | 1–2 ي |
| K14 | تسجيل دخول بديل بلا بيانات اعتماد من الكاش المحلي + هويات وهمية `usr-/biz-` غير موجودة في `auth.users` | 07/08 — `auth_controller.dart:104,128-138,155,238-289,444` | 1–2 ي |
| K15 | استعادة الجلسة من SharedPreferences بلا توكن ولا تحقق — انتحال هوية trivial | 07 — `auth_controller.dart:71-94` | س |
| K16 | شاشة العميل بأكملها بيانات وهمية ثابتة — تأكيد القيود/الربط/النزاعات/الكشف المجمع غير منفذة | 08 — `customer_home_screen.dart:93-104,181-195,264-272` | 3–5 ي |
| K17 | قراءة/كتابة `profiles` بعمودي `user_type/phone` غير الموجودين وبلا منحة INSERT | 08 — `auth_controller.dart:118-119,230-235,416-421` | س |
| K18 | `getMembers` يقرأ أعمدة `member_role/permissions/is_active` غير الموجودة → شاشة الفريق فارغة دائماً | 08 — `merchant_repository.dart:385-391` | س |
| K19 | `getDisputes` بتضمين `profiles!customer_id` غير الصالح → قائمة النزاعات لا تعمل أونلاين | 08 — `merchant_repository.dart:295-300` | س |
| K20 | `getDisputeMessages` بتضمين `profiles` بلا FK → محادثة النزاع فارغة دائماً | 08 — `merchant_repository.dart:333-336` | س |
| K21 | المرفقات لا تُرفع إلى Storage إطلاقاً — «سند موثق بالمرفقات» غير موجود فعلياً | 08 — `attachment_picker_widget.dart`؛ `create_ledger_entry_sheet.dart:84` | 1–2 ي |
| K22 | تناقض معماري: واجهة تعدد عملات (YER/SAR/USD) مقابل حارس العملة الواحدة `validate_ledger_entry_insert` | 01/02/06/08 — `0010:329-330`؛ `create_ledger_entry_sheet.dart:43-47,90` | قرار + 3–5 ي |
| K23 | `process-automation-rules` مكسور وقت التشغيل: أعمدة `reminders` غير موجودة، `disputes.status` وهمي، إشعارات تتجاوز outbox فلا Push إطلاقاً | 09 — `process-automation-rules/index.ts:213-221,274,225-237` | 1–2 ي |
| K24 | لا اختبارات RLS سلبية إطلاقاً (عزل المستأجرين جوهر المنتج) | 10 — `tests/database/001_schema_contract.sql:11-13`؛ `README.md:94-103` | 2–3 ي |
| K25 | لا اختبارات لأي من Edge Functions التسع، واختبار موبايل وحيد smoke من 15 سطراً مقابل 53 ملف Dart | 10 — `supabase/functions/` (لا `*test*`)؛ `mobile/test/widget_test.dart` | 3–5 ي |
| K26 | لا CI إطلاقاً — لا بوابة جودة ولا lint ولا تشغيل اختبارات عند الدمج | 10 — لا `.github/workflows`؛ `README.md:44-49` | 1 ي |

### 4.2 الملاحظات العالية (46 بنداً موحداً من 51 ملاحظة أصلية)

| # | الوصف | المصدر (تقرير — ملف:سطر) | الجهد |
|---|---|---|---|
| H1 | تسابق العكس مقابل الإنشاء: قفلان مختلفان يسمحان برصيد سالب (−100) رغم نجاح كل فحص على حدة | 02 — `0006:267` مقابل `0006:314` | 1 ي |
| H2 | idempotency معلن لثمانية أوامر ومنفذ لثلاثة؛ `post_manual_journal` بلا `client_request_id` أصلاً | 02/05 — `0011:5-10`؛ `0010:410-436` | 1–2 ي |
| H3 | كشف الحساب ليس لقطة متسقة (ثلاثة استعلامات بلا قفل) وبلا idempotency — كشف «موقّع» قد لا يطابق بنوده | 02 — `0008:152-180` | 1 ي |
| H4 | دوال أُنشئت بعد migration الصلاحيات 0003 تحتفظ بمنح `EXECUTE TO PUBLIC` الافتراضي | 02/03 — `0003:33` وكل migrations اللاحقة | 1 ي |
| H5 | حذف CASCADE في `customer_currency_balances` يكسر سياسة RESTRICT المعتمدة في الجداول المالية | 01 — `0012:15-16` | س |
| H6 | حالة `voided` لليوميات غير قابلة للوصول (تريجر يمنع أي UPDATE) وميزان المراجعة لا يرشّح بالحالة | 01/02/05 — `0010:10,121-124,465-477` | س |
| H7 | رصيد مشتق `customer_currency_balances` بلا حماية تعديل ولا وظيفة مصالحة — انحراف صامت إلى الأبد | 01 — `0012:13-26` | 1 ي |
| H8 | سياسة `service_role` ميتة (يتجاوز RLS أصلاً) توحي بحماية زائفة ولا تترك مسار تصحيح للأرصدة | 01 — `0012:45-48` | س |
| H9 | تخويل رفع الملفات مبني على صلاحية القراءة ثم كتابة بـ service_role — العميل read-only وviewer يرفقان ملفات بأي قيد | 03/04 — `signed-document-upload/index.ts:10-14`؛ `finalize-document-upload/index.ts:9-13` | 1 ي |
| H10 | باقة `business-assets` عامة وتسمح بـ `image/svg+xml` — XSS مخزّن محتمل | 03 — `202607120004:19` | س |
| H11 | تجاوز rate limiting في `verify-statement` عبر ترويسة `x-forwarded-for` مزوّرة + تعداد رموز 48-بت يكشف أرصدة | 04 — `verify-statement/index.ts:13-14` | س |
| H12 | `p_key_version` ثابتة =1 في `customer-directory` تكسر تدوير مفاتيح تشفير الهواتف | 04 — `customer-directory/index.ts:35` | س |
| H13 | فشل توكن جزئي في outbox يفشّل الإشعار كله → إعادة إرسال مكررة على أجهزة المستخدم | 04 — `process-notification-outbox/index.ts:169-216` | س |
| H14 | قبول نزاع `partially_accepted` على قيد خصم يفشل دائماً — لا مسار إغلاق نظيف | 02/05 — `0006:463-468`؛ `0010:356` | س |
| H15 | وصف القيد اليومي (بادئة + وصف القيد) قد يتجاوز 500 حرف → سقوط العملية كلها | 05 — `0010:260` مقابل `0010:68` و`0001:167` | س |
| H16 | اختبارات المحاسبة مسار سعيد فقط: العكس/قفل الفترة/الرفض السلبي/عدم القابلية للتعديل بلا اختبار | 05 — `tests/database/003_double_entry_accounting.sql` | 1–2 ي |
| H17 | الـ views ودوال الكشوفات تجمع مبالغ بعملات مختلفة بلا `group by currency_code` — مجاميع بلا معنى في كشوفات موقعة | 06 — `0002:653-670`؛ `0010:479-490`؛ `0008:152-164` | 1–2 ي |
| H18 | `journal_entry_lines` بلا عمود عملة — دفتر الأستاذ وميزان المراجعة يجمعان عملات مختلفة في حساب واحد | 01/06 — `0010:76-88` | 1–2 ي |
| H19 | أرصدة العملات المحلية لا تُزامَن من السيرفر إطلاقاً والتحديث التفاؤلي يتجاهل `reversal/opening_balance` | 06/07 — `sync_engine.dart:187-193`؛ `app_database.dart:405-411` | 1–2 ي |
| H20 | إجماليات لوحة التاجر تجمع عملات مختلفة معروضة برمز عملة واحد — تضليل مالي مباشر | 06 — `merchant_controller.dart:122-124`؛ `merchant_home_screen.dart:447-456` | س |
| H21 | السحب (Pull) لا يجلب `ledger_entries`/النزاعات/الكشوفات + كتابة فوقية بلا حل تعارضات — جهاز ثانٍ لن يرى أي قيد | 07/08 — `sync_engine.dart:147-220`؛ `app_database.dart:303-310` | 2–3 ي |
| H22 | كتابة مباشرة `profiles.upsert` من العميل — خرق لمبدأ API-only | 07 — `auth_controller.dart:230,416` | س |
| H23 | حذف طابور العمليات غير المرسلة عند تسجيل الخروج — فقدان بيانات مالية بصمت بلا تحذير | 07 — `auth_controller.dart:505`؛ `app_database.dart:511-520` | س |
| H24 | لا حد أقصى لإعادة المحاولة ولا dead-letter ولا تمييز أخطاء 4xx الدائمة — طابور يعلق للأبد | 07/08 — `app_database.dart:496-503`؛ `sync_engine.dart:116-143` | 1 ي |
| H25 | `occurred_at` من ساعة الجهاز القابلة للتعديل — تلاعب بتواريخ القيود والاستحقاق | 07 — `merchant_repository.dart:101,141,168,236` | س |
| H26 | التخزين المحلي غير مشفر: SQLite بلا SQLCipher + أسرار في SharedPreferences + `pendingPassword` نص صريح | 07 — `app_database.dart:11`؛ `supabase_config.dart:45-48`؛ `auth_controller.dart:22,56` | 1–2 ي |
| H27 | معرّفات محلية مؤقتة (`entry-/cust-/biz-`) لا تُسوَّى مع معرّفات السيرفر بعد النجاح | 07 — `merchant_repository.dart:54,100`؛ `auth_controller.dart:155` | 1–2 ي |
| H28 | صفر استدعاءات لكل Edge Functions التسع من الموبايل — لا PDF، لا تشفير هاتف، لا رفع مرفقات، لا تحقق كشوف | 08 — `functions.invoke` = 0 نتائج في `mobile/lib` | 2–3 ي |
| H29 | قيمة `p_scope: 'customer'` خاطئة — الـ enum يقبل `business_customer/customer_consolidated` فقط | 08 — `merchant_repository.dart:453`؛ `0001:31` | س |
| H30 | `inviteMember` يرسل `p_phone` في خانة `p_target_user_id uuid` — خلط مفاهيمي كامل | 08 — `merchant_repository.dart:422-426`؛ `invite_member_sheet.dart:114-132` | س |
| H31 | تجاهل بنية الأوفلاين الخلفية: لا تسجيل جهاز، لا checkpoints، لا قراءة إيصالات | 08 — `0006:5-39`؛ `0011:5-10` | 1–2 ي |
| H32 | مسارات ميتة في محرك المزامنة: `confirm_ledger_entry`/`open_dispute` لا يُدرجهما أي كود في الطابور | 07/08 — `sync_engine.dart:128-132` | 1 ي |
| H33 | عمليات النزاع أونلاين فقط بلا طابور وتفشل بصمت برسالة مضللة «تحقق من اتصالك» | 08 — `merchant_repository.dart:346-377` | 1 ي |
| H34 | وثيقتان مرجعيتان تدّعيان `generate-statement` غير منفذ (501) بينما هو منفذ بالكامل — قرار إطلاق خاطئ الاتجاهين | 09 — `api-reference-and-operational-flow.md:127`؛ `database-architecture-and-operations-spec.md:21,290` | س |
| H35 | الوثائق تدّعي `process-automation-rules` معطّل بينما هو «مفعّل ظاهرياً وفاشل فعلياً» — ثقة زائفة | 09 — `api-reference-and-operational-flow.md:126` | س |
| H36 | PDF كشف الحساب إنجليزي فقط بخط Helvetica — لا عربية ولا RTL في وثيقة مالية موقعة لسوق عربي | 09 — `generate-statement/index.ts:65-66,86,235-241` | 2–3 ي |
| H37 | `cleanup-expired-data` موثقة وغير موجودة + `upload_sessions` المنتهية بلا منظّف | 09 — `first-version-product-architecture-spec.md:404`؛ `0006:128` | 1 ي |
| H38 | تعدد العملات غير مختبر إطلاقاً (لا نتيجة لبحث `currency` في الاختبارات) | 06/10 — `supabase/tests/database/` | 1–2 ي |
| H39 | لا اختبارات تزامن رغم طلبها صراحة في المواصفة | 10 — `First-version.md:1536` | 1–2 ي |
| H40 | عدم القابلية للتعديل/الحذف ومنع عكس العكس غير مختبرة مباشرة | 10 — `README.md:99` | 1 ي |
| H41 | تناقض سياسة «الدفع الأكبر من الرصيد»: الوثائق تقول المنع والتنفيذ يسمح رصيداً دائناً افتراضياً | 02/09/10 — `First-version.md:1037-1043`؛ `0010:39,366-367`؛ `003:33` | 1 ي (قرار + توحيد) |
| H42 | سيناريوهات النزاعات ناقصة (رفض، منع تكرار، تصعيد) | 10 — `README.md:102`؛ `First-version.md:782` | 1–2 ي |
| H43 | صلاحيات الأدوار غير مختبرة سلبياً (cashier، تأكيد نيابة، عميل غير مرتبط) | 10 — `First-version.md:1011` | 1–2 ي |
| H44 | مسار المصادقة الإنتاجي غير محسوم: SMS معطل + بريد معطل + اعتماد على OpenWA يشير localhost | 10 — `config.toml:55,59-61`؛ `send-whatsapp-otp/index.ts:3` | 2–3 ي |
| H45 | لا استراتيجية نسخ احتياطي/استعادة موثقة رغم كونها ميزة معلنة ومتطلب اعتمادية | 10 — `First-version.md:102,1380` | 1 ي |
| H46 | لا مراقبة إطلاقاً: لا Sentry/Crashlytics ولا تنبيهات للدوال وcron | 10 — `First-version.md:1717-1722`؛ `pubspec.yaml` | 1–2 ي |

---

## 5. خارطة طريق الإطلاق

خطة إصلاح على ثلاث مراحل، مبنية من توصيات الأولوية في التقارير العشرة (خاصة قوائم «توصيات الإصلاح» في 01/03/04/06/07/08/09 و«قائمة فجوات الإطلاق» في 10). تقدير تقرير 10 المرجعي: جاهزية «تجريبي مغلق» = 2–3 أسابيع عمل مركّز.

### المرحلة صفر — إصلاحات تحظر الإطلاق (لا يجوز نشر أي شيء قبلها)

**الهدف:** نظام قابل للنشر، يكتب مالياً من التطبيق فعلاً، وبلا أسرار مسربة ولا التفاف مصادقة.
**الجهد التقديري:** ~2–3 أسابيع (فريق 2–3 مهندسين).

| # | البند | البنود المرتبطة |
|---|---|---|
| 0.1 | **تدوير مفتاح OpenWA فوراً** وإزالة كل القيم الافتراضية من `send-whatsapp-otp/index.ts:4-5` و`whatsapp_otp_service.dart:28-29` + فحص أسرار شامل للمستودع | K6, K8 |
| 0.2 | **حسم الترحيل 0012:** حذفه أو إعادة كتابته من الصفر (تعريف/استبدال `has_business_permission`، تصحيح الاتجاهات إلى `debit/credit`، FK إلى `customers(id)`، فحص أدوار + revoke + `search_path`، إزالة `'fee'`) — مع حسم القرار المعماري: عملة واحدة لكل محل (وإزالة محدد العملات من الموبايل) أو تعدد حقيقي (وتحديث 0010 والمحاسبة والـ views) | K1–K5, K22 |
| 0.3 | **توحيد عقد RPC:** تصحيح أسماء الدوال الأربع، توحيد توقيع `create_ledger_entry`، تصحيح `p_entry_id` و`p_scope` و`p_target_user_id`، وإزالة/إضافة `p_currency_code` في الخصم — ثم اختبار تكامل end-to-end إلزامي لكل أمر | K10–K12, H29, H30 |
| 0.4 | **نقل OTP إلى الخادم بالكامل** (توليد/تخزين/تحقق في Edge Function + جدول private بمهلة ومحاولات محدودة) وحذف كودي `123456/000000` وقبول `mockOtpCode` | K7, K8 |
| 0.5 | **إزالة مسارات الالتفاف في المصادقة:** منع الدخول دون جلسة Supabase صالحة، حذف fallback الكاش المحلي، ومنع استعادة الجلسة من SharedPreferences بلا توكن | K14, K15 |
| 0.6 | **إصلاح IDOR في `generate-statement`:** تحقق ملكية الكشف عبر عميل المستخدم (RLS) قبل الجلب بمعرّف | K9 |
| 0.7 | **ربط إضافة العميل بالباكند:** استدعاء `add_business_customer` + إدراج الأمر في طابور المزامنة + تسوية المعرّفات المحلية بمعرّفات السيرفر | K13, H27 |
| 0.8 | **إصلاح الاستعلامات المكسورة:** أعمدة `business_members` الفعلية، تضمينات النزاعات وفق FKs الحقيقية، إسقاط `user_type/phone` من `profiles` | K17–K20 |
| 0.9 | **إيقاف ابتلاع الأخطاء:** إظهار فشل العمليات للمستخدم وتمييز 4xx الدائمة (خروج من الطابور بحالة «فشل نهائي») عن أخطاء الشبكة، مع حد أقصى وbackoff | H24, H33 |
| 0.10 | **إنشاء CI أولي:** `supabase db lint` + `supabase test db` + `flutter analyze` + `flutter test` عند كل PR — قاعدة «لا إطلاق بلا بوابة» | K26 |

### المرحلة 1 — جاهزية تجريبي مغلق (5–10 محلات وفق `First-version.md:1574-1580`)

**الهدف:** ثقة تشغيلية: اختبارات نفي، مزامنة ثنائية الاتجاه، مراقبة، ونسخ احتياطي.
**الجهد التقديري:** ~3–4 أسابيع إضافية.

| # | البند | البنود المرتبطة |
|---|---|---|
| 1.1 | اختبارات pgTAP سلبية: تحويل كل خلية «مرفوض» في `docs/rls-matrix.md` إلى اختبار (عزل المستأجرين، عميل/عميل، أدوار) | K24, H43 |
| 1.2 | اختبارات وحدة لمنطق المزامنة والطابور وidempotency في الموبايل + اختبارات Deno للدوال الحرجة (التشفير، verify-statement، finalize-upload) | K25 |
| 1.3 | إكمال المزامنة ثنائية الاتجاه: سحب `ledger_entries` و`customer_currency_balances` والنزاعات، تحديث `sync_status` بعد النجاح، معالجة `reversal` في الرصيد التفاؤلي، استعلام مجمّع بدل N+1 | H19, H21, H32 |
| 1.4 | إصلاح `process-automation-rules` (أعمدة `reminders` الصحيحة، `dispute_state`، `enqueue_notification`) أو تعطيله صراحة وتوثيقه | K23 |
| 1.5 | تشديد تخويل الرفع إلى فحص دور كتابة + إزالة `image/svg+xml` من `business-assets` + فحص magic bytes | H9, H10 |
| 1.6 | إصلاح تسابق العكس/الإنشاء (قفل صف `business_customers` في العكس) ولقطة كشف الحساب (قفل أو عزل أعلى) وإكمال idempotency للأوامر الخمسة الناقصة | H1, H2, H3 |
| 1.7 | حسم سياسة «الدفع الأكبر من الرصيد» وتوحيد الوثائق والتنفيذ والاختبارات | H41 |
| 1.8 | خطة نسخ احتياطي + اختبار استعادة موثق + مراقبة (Crashlytics/Sentry + تنبيهات دوال وcron) | H45, H46 |
| 1.9 | حسم مسار OTP الإنتاجي (مزود موثق، أسرار، اختبار طرف-لطرف) + حماية `WORKER_SECRET` وإصلاح rate limit في `verify-statement` | H44, H11 |
| 1.10 | حماية البيانات المحلية: تشفير SQLite، منع حذف الطابور عند signOut قبل المزامنة (أو تحذير صريح)، `now()` من السيرفر لتواريخ القيود | H23, H25, H26 |
| 1.11 | بيانات بذرة لبيئة staging + تشغيل المرحلة التجريبية المغلقة مع فحص صحة يومي آلي (مطابقة `business_customer_balances` مقابل مجموع القيود) | 10 توصية 4 |

### المرحلة 2 — إطلاق تجاري

**الهدف:** اكتمال المنتج وظيفياً وتوثيقياً وتشغيلياً.
**الجهد التقديري:** ~4–6 أسابيع إضافية (تتضمن تشغيل التجريبي المغلق بنجاح).

| # | البند | البنود المرتبطة |
|---|---|---|
| 2.1 | تنفيذ واجهة العميل فعلياً: ربط `customer_business_summary` و`confirm_ledger_entry` و`respond_link_request` و`open_dispute` وإدراج أوامر العميل في الطابور | K16, H32 |
| 2.2 | ربط Edge Functions بالواجهة: `generate-statement` (PDF حقيقي وختم SHA-256)، `verify-statement` (شاشة تحقق)، `bootstrap-user-contact`/`customer-directory` (ربط العملاء بالهاتف)، رفع المرفقات عبر `signed-document-upload`/`finalize-document-upload` | K21, H28 |
| 2.3 | حل مشكلة عربية PDF (تضمين خط عربي TTF + دعم RTL/تشكيل) وإصلاح `Math.abs` في عمود الرصيد الجاري | H36 |
| 2.4 | حسم تعدد العملات نهائياً: إما إزالة الواجهة أو تنفيذ كامل (عملة في journal lines، تجميع الـ views والكشوفات حسب العملة، مزامنة الأرصدة، عرض مجزأ بدل الإجمالي المختلط، استبدال `'ر.ي'` الثابتة) | K22, H17–H20 |
| 2.5 | اختبارات تزامن وأداء وعدم قابلية تعديل + استكمال سيناريوهات النزاعات + اختبارات تعدد العملات | H16, H38–H40, H42 |
| 2.6 | تحديث التوثيق الشامل: `api-reference` و`database-architecture-spec` و`edge-functions.md` و`erd.md` و`full_schema.sql` (إعادة توليد آلياً) و`mobile/README.md` + اختبار pgTAP يقارن المخطط الفعلي بالمرجع في CI | H34, H35 |
| 2.7 | سد الثغرات المتوسطة المتراكمة: revoke صريح من PUBLIC/anon لكل دالة جديدة، توحيد سياسات `link_status`، `cleanup-expired-data` أو منظّف `upload_sessions`، منع الفترات المفتوحة المتداخلة، `updated_at` لـ `sync_checkpoints`، حالة `voided` (قيد عكسي أو استثناء)، مصالحة دورية للأرصدة | H4–H8, H37 |
| 2.8 | توثيق تشغيلي (runbook): استعادة، تدوير أسرار، نزاع مصعد، إيقاف حساب مسيء + feature flags للتجريبي | 10 توصية 6 |

---

## 6. نقاط القوة — ما هو مبني باحترافية ويستحق الثقة

رغم 212 ملاحظة سلبية، سجّلت التقارير **75 نقطة إيجابية** تؤكد أن النواة سليمة وقابلة للبناء عليها:

### قاعدة البيانات والمخطط (تقارير 01، 02، 05)
- **لا فاصلة عائمة إطلاقاً:** كل المبالغ `numeric(20,4)` — دقة كاملة بلا تقريب (0001:129,165,350-353؛ 0010:83-84).
- **سجل مالي append-only حقيقي:** تريجرات `prevent_update_delete` على كل جداول الوقائع المالية السبعة (0001:442-452).
- **توازن القيد المزدوج مفروض قاعدياً:** constraint trigger مؤجل `trg_journal_entry_balanced` (0010:204-221) يمنع commit أي قيد غير متوازن — ضمان على مستوى قاعدة البيانات لا التطبيق، مع منع القيود اليتيمة (0010:223-237) وذرّية كاملة (مستحيل قيد تشغيلي بلا يومية).
- **الترحيلات المحاسبية صحيحة نوعاً نوعاً** (افتتاحي/دين/سداد/خصم/عكس) — تحقق يدوي مطابق للمواصفة والاختبار (05§أولاً).
- **Idempotency بطبقتين:** `unique(business_id, client_request_id)` (0001:176) + `command_receipts` (0006:19-28) مع فحص مسبق بعد قفل صف العميل — إعادة التنفيذ آمنة حتى تحت التزامن.
- **سلامة العكس:** منع عكس العكس، منع العكس المزدوج، إلزام تطابق المبلغ والعملة وعكس الاتجاه (0010:335-341).
- **نمط أوامر سليم:** `private.command_*` بـ `security definer` + `set search_path=''` + أغلفة عامة رفيعة مع دفاع بالعمق عبر `validate_ledger_entry_insert`.
- **Outbox تعاملي احترافي:** إشعار + outbox في نفس المعاملة، سحب بـ `FOR UPDATE SKIP LOCKED`، تراجع أسّي، dead-letter بعد 8 محاولات (0002:88-111؛ 0005:46-77؛ 0006:216-239).

### الأمان (تقرير 03)
- **RLS مفعّل على كل الجداول العامة الـ 31 بلا استثناء** (مطابقة كاملة `create table` ↔ `enable row level security`).
- **Default-deny منهجي:** سحب شامل ثم منح SELECT فقط، وكل الكتابات عبر RPCs محصّنة (0003:30-53).
- **عزل PII الهاتف:** تشفير AES-256-GCM + HMAC-SHA256 للبحث في `private.customer_contacts` (0001:80-91) — بنية صحيحة بتطبيع E.164 صارم.
- **كل الـ views الخمسة بـ `security_invoker=true`** — لا تجاوز لـ RLS (0002:653-679؛ 0010:465-480).
- **Storage منضبط:** باقات خاصة افتراضياً، الرفع عبر signed URL بعد فحص وصول، حدود حجم وأنواع MIME، جلسات رفع تنتهي خلال 15 دقيقة وتُستهلك مرة واحدة.
- **Realtime محدود النطاق** — لا جداول PII ولا قيود خام (0004:99-117)؛ عمال الخلفية محميون بـ `requireWorkerSecret` وCORS مقيد.

### Edge Functions (تقرير 04)
- `requireUser` يتحقق من JWT فعلياً عبر `auth.getUser` في 7 من 9 دوال؛ فصل صلاحيات least-privilege منضبط.
- rate limiting مركزي ذرّي صحيح تزامنياً (`service_consume_rate_limit`)؛ قواعد الأتمتة idempotent عبر `run_key`؛ حسابات الكشف في قاعدة البيانات سليمة رياضياً (رصيد افتتاحي + مدين − دائن بترتيب حتمي).

### الموبايل (تقرير 07)
- **مبدأ API-only مطبَّق فعلياً على كل العمليات المالية** — لا كتابة مباشرة على الجداول المحصّنة (الاستثناء الوحيد `profiles.upsert`).
- **الحفظ التفاؤلي + إدراج الطابور في معاملة SQLite واحدة** — لا قيد محلي بلا أمر مرتبط (app_database.dart:371-474).
- حارس تزامن `_isProcessing` وطابور FIFO وبنية Repository/Database/SyncEngine قابلة للصيانة.

### التوثيق والاختبارات (تقارير 09، 10)
- **كل RPCs الموثقة في مرجع API منفذة فعلاً بنفس الأسماء والمعاملات والأدوار** (09 P-1)، والمبادئ المالية غير القابلة للتنازل منفذة كما وُثقت (P-2)، ومصفوفة RLS الموثقة تطابق التنفيذ (P-3).
- **31 اختبار pgTAP جيدة الصنع:** حتمية، بلا تلويث بيانات، تحاكي مستخدمين حقيقيين تحت RLS، وتغطي المسار السعيد الكامل بما فيه idempotency والنزاع الجزئي والقيد المزدوج المتوازن.
- `.env.example` موثق بعناية بلا أسرار حقيقية، و`config.toml` يعطل الدخول المجهول ويفرض كلمة مرور ≥ 12.

---

## 7. خاتمة

الصورة النهائية متسقة عبر التقارير العشرة: **نواة خلفية من الطراز الأول (0001–0011) محاصرة بطبقة أخيرة مكسورة (0012)، وواجهة أمامية غير موصولة فعلياً بالباكند، ومصادقة شكلية، وسر مسرّب، وغياب بوابات جودة.** لا توجد ملاحظة حرجة واحدة غير قابلة للإصلاح — كلها ذات طبيعة «أسابيع عمل» لا «إعادة تصميم». بتنفيذ المرحلة صفر (~2–3 أسابيع) يصبح النظام قابلاً للنشر والكتابة الحقيقية؛ وبالمرحلتين 1 و2 يصل إلى جاهزية تجارية كاملة. أول إجراءين اليوم: **تدوير مفتاح OpenWA** (ساعات) و**تجميد الترحيل 0012** (قرار إداري فوري).

---

*أُعدّ هذا التقرير بدمج محتوى التقارير العشرة في `analysis-reports/` حصراً دون إضافة ملاحظات جديدة؛ كل مراجع الملفات وأرقام الأسطر منقولة من تلك التقارير وقابلة للتحقق فيها.*

**ظهر في 7 تقارير من 10** (01، 02، 03، 05، 06، 08، 09). الأدلة المجمعة:

- **لن يُطبَّق أصلاً:** السياسة `customer_currency_balances_select_member` تستدعي `public.has_business_permission(business_id, 'view_balances'::public.business_permission)` وكلاهما غير معرّف في أي ملف بالمشروع — `202608180012_multi_currency_and_categories.sql:38-43` (01§1.1، 02§2، 03 ح-3، 05 حرج-1، 06 حرج-1، 08 C-6، 09 C-1). الملف أيضاً بلا غلاف `begin/commit` بخلاف 0001/0006/0010 (01§1.1، 02§2).
- **لو طُبِّق لعطّل كل الكتابة المالية:** التريجر `private.trig_update_customer_currency_balance` مربوط `after insert on public.ledger_entries` (0012:108-111) ويقارن `new.direction = 'in'` (سطر 59) بينما `public.ledger_direction` قيماه `('debit','credit')` فقط (`202607120001_core_schema.sql:20`) → خطأ `22P02 invalid input value for enum` عند كل إدراج قيد (01§1.2، 02§1، 03 ح-4، 05 حرج-1، 06 حرج-2).
- **مفتاح أجنبي للجدول الخاطئ:** `customer_currency_balances.customer_id references public.profiles(id)` (0012:17) بينما التريجر يدرج `new.customer_id` القادم من `ledger_entries.customer_id` الذي يشير إلى `public.customers(id)` (`202607120001_core_schema.sql:162`) → انتهاك FK شبه دائم (01§1.3، 02§1، 03 ح-4، 08 M-4، 09 C-1).
- **دالة عامة موازية بلا صلاحيات:** `public.command_create_ledger_entry` (0012:114-209) بلا أي فحص `is_business_member`، `security definer` بلا `set search_path=''`، بلا revoke (منح PUBLIC الافتراضي)، تستخدم `'fee'` غير الموجودة في `ledger_entry_type` (سطر 157)، `p_source_device_id text` في عمود `uuid` (سطر 128 مقابل 0001:173)، و`p_description default ''` يخالف قيد الطول (سطر 123 مقابل 0001:167) (01§1.4، 02§3، 03 ح-2، 05 حرج-1، 06 حرج-3، 09 C-1).
- **تناقض بنيوي مع 0010:** القيد `ledger_entries_check` المعدَّل في `202608140010:314-319` يشترط `debit/credit` بينما 0012 يدرج `'in'/'out'` — الترحيلان لا يمكن أن يعملا معاً (01§1.5، 06 حرج-3).
- **تناقض معماري مع حارس العملة الواحدة:** `validate_ledger_entry_insert` (0010:329-330) يفرض أن عملة كل قيد = عملة المحل، فجدول الأرصدة متعدد العملات لن يحمل إلا عملة واحدة عملياً (01§2.1، 02#6، 06 حرج-5، 08 C-7).
- **خارج كل مراجع الحقيقة:** غير موثق في أي وثيقة، و`full_schema.sql` (3070 سطراً) يتوقف عند 0011، ولا اختبار يغطيه (01§3.1، 06 منخفض-15، 09 C-1/L-4، 10 #4).

**الحكم المجمع:** يبدو أن 0012 كُتب ضد نسخة مخطط مختلفة تماماً لم تصل إلى هذا المستودع (01§1.5). لا يجوز دمجه بحالته الحالية؛ إما حذفه أو إعادة كتابته من الصفر بعد حسم القرار المعماري لتعدد العملات.

### X-2: مفتاح OpenWA مسرّب في الكود وفي حزمة التطبيق

**ظهر في 6 تقارير** (03، 04، 07، 08، 09، 10). الأدلة المجمعة:

- `supabase/functions/send-whatsapp-otp/index.ts:4-5`: كانت توجد قيم افتراضية حقيقية لـ OpenWA وتم حجبها (03 ح-1، 04 ح-1، 09 C-3، 10 #18).
- **نفس المفتاح مكرر حرفياً في الموبايل:** `mobile/lib/core/services/whatsapp_otp_service.dart:28-29` كثوابت `const` — يُستخرج من أي APK/IPA (03 ح-1، 04 ح-1، 07 ح-6، 08 C-10).
- عناوين شبكة داخلية ثابتة للتطوير في `whatsapp_otp_service.dart:32-36` (`http://192.168.0.134:2785/api`، localhost، 10.0.2.2) (03 ح-1، 07 ح-6، 08 C-10).
- الدالة نفسها بلا مصادقة وبلا rate limit وCORS مفتوح `"*"` (`send-whatsapp-otp/index.ts:13-79`)، وغير مدرجة في `config.toml` `[functions.*]` — أي مجهول يرسل رسائل واتساب باسم النشاط (03 ع-1، 04 ح-2، 09 C-3).
- تناقض توثيقي: الدالة غير موثقة في أي قائمة، وتخالف النطاق الموثق صراحة («WhatsApp/SMS جماعي مدفوع» خارج النطاق — `first-version-product-architecture-spec.md:65`) (09 C-3).

**الإجراء المجمع:** إبطال المفتاح فوراً، إزالة كل القيم الافتراضية، نقله إلى أسرار Edge Function فقط، منع اتصال العميل ببوابة واتساب مباشرة، وإضافة فحص أسرار (gitleaks/trufflehog) إلى CI (10 توصية 5).

### X-3: عدم تطابق عقد RPC بين الموبايل والباكند

**ظهر في 5 تقارير** (02، 05، 06، 07، 08). الأدلة المجمعة:

- **`create_ledger_entry`:** الموبايل يرسل 12 معاملاً تشمل `p_currency_code, p_category, p_payment_method, p_reference_number, p_bank_or_agent_name, p_attachment_path` (`merchant_repository.dart:129-143`، يُرسل عبر `sync_engine.dart:123`) بينما `public.create_ledger_entry` تقبل 9 معاملات فقط بلا أي منها (`202607120002:413-425`) → `PGRST202` دائم (05 حرج-2، 06 حرج-4، 07 ح-1، 08 C-2).
- **`apply_customer_discount`:** الموبايل يرسل `p_currency_code` (`merchant_repository.dart:190-196`) غير الموجود في التوقيع (`202608140010:404-408`) (05 حرج-2، 06 حرج-4، 07 ح-1، 08 C-4).
- **`reverse_ledger_entry`:** الموبايل يرسل `p_original_entry_id` (`merchant_repository.dart:259-263`) والدالة تتوقع `p_entry_id` (`202607120002:453`) (07 ح-1، 08 C-3).
- **أربع دوال بأسماء غير موجودة إطلاقاً:** `command_add_dispute_message` (سطر 349)، `command_resolve_dispute` (367، مع `p_note` بدل `p_resolution_note`)، `command_invite_business_member` (422، مع `p_phone` بدل `p_target_user_id`)، `command_generate_statement` (452) — الصحيح: `add_dispute_message` (0002:563)، `resolve_dispute` (0002:606)، `invite_business_member` (0008:60)، `create_statement` (0008:185) (02#23، 07 ح-2، 08 C-1).
- **قيمة scope خاطئة:** `p_scope: 'customer'` (`merchant_repository.dart:453`) بينما enum `statement_scope` قيمتاه `'business_customer'/'customer_consolidated'` (0001:31) (08 H-2).
- **استعلامات مباشرة مكسورة:** `getMembers` يقرأ أعمدة `member_role, permissions, is_active` غير الموجودة (الفعلية `role, status` — `merchant_repository.dart:385-391`)؛ `getDisputes` يضمّن `profiles!customer_id` بلا FK (295-300)؛ `getDisputeMessages` يضمّن `profiles` بلا FK (333-336)؛ `profiles` يُقرأ/يُكتب بعمودي `user_type/phone` غير الموجودين (`auth_controller.dart:118-119,230-235,416-421`) (08 C-11/C-12/C-13/C-14).
- **الأثر المركّب:** كل عملية مالية أوفلاين تبقى في `offline_mutations_queue` للأبد تُعاد كل 30 ثانية وتفشل بصمت (الخطأ في `debugPrint` فقط — `sync_engine.dart:137`)، والواجهة تعرض أرصدة محلية كأنها موثقة (07 ح-1)؛ والنزاعات والدعوات والكشوفات معطلة 100% مع ابتلاع الاستثناءات وإرجاع `false/null` بصمت (07 ح-2، 08 C-1).

### X-4: مصادقة OTP والجلسات على جهاز العميل — لا قيمة أمنية

**ظهر في 5 تقارير** (03، 04، 07، 08، 10). الأدلة المجمعة:

- توليد الرمز على الجهاز (`whatsapp_otp_service.dart:95-99`)، التخزين في ذاكرة التطبيق `_activeOtps` (سطر 39)، والتحقق محلياً (سطور 188-241) (03 ح-5، 04 ح-2).
- كودا تجاوز ثابتان `'123456'` و`'000000'` يُقبلان دائماً (`whatsapp_otp_service.dart:196-199`)، والرمز الافتراضي `'123456'` معرّف في `AuthState` نفسه (`auth_controller.dart:36`) (07 ح-3، 08 C-10).
- قبول `state.mockOtpCode` عند فشل الخدمة — والرمز «السري» مخزن أصلاً في حالة التطبيق (`auth_controller.dart:397`، و315-324) (07 ح-3، 08 C-10).
- **تسجيل دخول بديل بلا بيانات اعتماد:** عند فشل `signInWithPassword` لأي سبب يُقبل الدخول من الكاش المحلي بلا مقارنة كلمة سر (`auth_controller.dart:128-138`)، و`registerWithPassword` ينشئ حساباً محلياً وهمياً عند فشل السيرفر (238-289) (07 ح-4، 08 C-9).
- **استعادة جلسة بلا توكن:** وجود `cached_user_id` في SharedPreferences يكفي لاعتبار المستخدم `authenticated` (`auth_controller.dart:71-94`) — انتحال هوية trivial على جهاز rooted (07 ح-5).
- لا يوجد تحقق هاتف بديل من مزوّد: `config.toml:59-61` يعطّل SMS OTP، و`:55` يعطّل التسجيل بالبريد (03 ح-5، 10 #19).

### X-5: غياب CI والاختبارات — لا بوابة جودة إطلاقاً

**ظهر في 4 تقارير** (05، 06، 09، 10). الأدلة المجمعة:

- **لا CI إطلاقاً:** لا `.github/workflows` ولا أي pipeline في المشروعين (بحث شامل عن `*.yml`) — التشغيل محلي فقط موثق في `README.md:44-49` (10 #16).
- **31 تأكيد pgTAP فقط** في 3 ملفات، كلها مسار سعيد: لا اختبار RLS سلبي واحد رغم أن `README.md:94-103` يسردها كموصى بها (10 #1)؛ لا اختبار لأي Edge Function (10 #2)؛ اختبار موبايل وحيد smoke من 15 سطراً مقابل 53 ملف Dart (10 #3).
- **فجوات محاسبية:** لا اختبار للعكس (reversal)، ولا لرفض القيد غير المتوازن، ولا لقفل الفترات، ولا لعدم القابلية للتعديل (05#5، 10 #6).
- **تعدد العملات بلا أي اختبار** — البحث عن `currency` في مجلد الاختبارات بلا نتائج (06 توصية 6، 10 #4).
- **لا اختبارات تزامن** رغم أن `First-version.md:1536` يطلبها صراحة (10 #5).
- **الترحيل 0012 خارج كل مراجع الحقيقة:** وثائق/مخطط مجمع/اختبارات (09 L-4).

---

## 3. أقسام المجالات العشرة (خلاصة كل تقرير)

### 3.1 المخطط والقيود (Schema & Constraints) — التقرير 01

**الخلاصة:** المخطط الأساسي (0001–0011) متماسك ومهني: مبالغ `numeric(20,4)` بلا فاصلة عائمة، `timestamptz` في كل مكان، سجل مالي append-only، قيود CHECK شاملة، وفهارس جيدة. الخلل كله في الترحيل الأخير 0012 المكسور بنيوياً (تفاصيله في X-1 أعلاه)، إضافة إلى أن `full_schema.sql` متوقف عند 0011.

**أهم الملاحظات (مرتبة حسب الخطورة):**
- [حرج] 0012 لن يُطبَّق (دالة/نوع غير موجودين، 0012:38-43)؛ تريجر يعطّل كل إدراج قيد (0012:51-111)؛ FK للجدول الخاطئ (0012:17)؛ دالة عامة بلا صلاحيات (0012:114-209)؛ تناقض مع قيد 0010 (0010:314-319).
- [عالي] تضارب مفاهيمي 0010/0012 حول تعدد العملات؛ حذف CASCADE في جدول مالي يكسر سياسة RESTRICT (0012:15-16)؛ حالة `voided` غير قابلة للوصول (0010:10,121-124)؛ رصيد مشتق بلا مصالحة ولا حماية (0012:13-26)؛ سياسة service_role ميتة توحي بحماية زائفة (0012:45-48).
- [متوسط] `full_schema.sql` غير محدث؛ `category/payment_method` نص حر بلا قيود (0012:5-10)؛ `attachment_url` مسار مرفقات موازٍ (0012:10)؛ تحويل صامت للعملة غير الصالحة إلى 'YER' (0012:147-150)؛ `sync_checkpoints.updated_at` بلا تريجر (0006:36)؛ قيود اليومية بلا عملة وفترات مفتوحة متداخلة ممكنة (0010:76-88,55).

**التقرير التفصيلي:** `analysis-reports/01-schema-constraints.md` (حرج 5 · عالي 5 · متوسط 6 · منخفض 6 · إيجابي 10).

### 3.2 الدوال والمشغلات وتسلسل العمليات — التقرير 02

**الخلاصة:** البنية الأساسية متينة (نمط أوامر `private.command_*` بأغلفة عامة رفيعة، idempotency صحيح، outbox تعاملي احترافي)، لكن 0012 كتلة مكسورة بالكامل، وتوجد تسابقات حقيقية وفجوات idempotency. ملاحظة منهجية: `private.command_create_ledger_entry` أُعيد تعريفها 4 مرات (0002←0006←0007←0010) والتحليل يستند للنسخة النهائية.

**أهم الملاحظات:**
- [حرج] ثلاثيتها كلها داخل 0012 (انظر X-1).
- [عالي] **تسابق العكس مقابل الإنشاء:** `command_create_ledger_entry` يقفل صف `business_customers` (0006:267) بينما `command_reverse_ledger_entry` يقفل صف القيد فقط (0006:314) — سداد + عكس متزامنان يدفعان الرصيد إلى −100 رغم نجاح كل فحص على حدة؛ والعكس بلا فحص رصيد أصلاً. **idempotency معلن لثمانية أوامر ومنفذ لثلاثة** (0011:5-10)؛ `command_post_manual_journal` لا يستقبل `client_request_id` أصلاً (0010:410-436). **كشف الحساب ليس لقطة متسقة** — ثلاثة استعلامات بلا قفل (0008:152-180) وبلا receipt. **دوال أُنشئت بعد 0003 تحتفظ بمنح PUBLIC الافتراضي** (0003:33).
- [متوسط] حارس «السداد يتجاوز الرصيد» معطّل افتراضياً (`allow_customer_credit_balance default true`، 0010:39)؛ قبول نزاع جزئي على خصم يفشل دائماً (0006:463-468 + 0010:356)؛ عكس قيد عليه نزاع نشط يعلّق النزاع (0006:302-336)؛ فجوات سجل التدقيق (لا تدقيق على `accounting_periods` و`chart_of_accounts` — 0005:37-43)؛ تسابق تأكيد/نزاع وتأكيد قيود معكوسة (0006:339-387).

**التقرير التفصيلي:** `analysis-reports/02-functions-logic.md` (حرج 3 · عالي 5 · متوسط 6 · منخفض 9 · إيجابي 7).

### 3.3 الأمان وRLS — التقرير 03

**الخلاصة:** النموذج العام default-deny سليم البنية: RLS مفعّل على كل الجداول العامة الـ 31 بلا استثناء، سحب شامل ثم منح انتقائية (0003:30-53)، عزل PII بتشفير AES-GCM، وكل الـ views بـ `security_invoker=true`. لكن ثغرات 0012 وطبقة OTP/واتساب تُبطل جزءاً من هذا الانضباط.

**أهم الملاحظات:**
- [حرج] مفتاح OpenWA مكشوف (X-2)؛ دالة 0012 العامة بلا تفويض (X-1)؛ سياسة 0012 غير القابلة للتطبيق (X-1)؛ تضارب FK/enum في 0012 (X-1)؛ التحقق من OTP على العميل (X-4).
- [عالي] `send-whatsapp-otp` إرسال اعتباطي بلا مصادقة (X-2)؛ **رفع المستندات يتحقق من القراءة فقط ثم يكتب بـ service_role** — العميل read-only ودور viewer يستطيعان إرفاق ملفات بأي قيد يقرأانه (`signed-document-upload/index.ts:10-14`، `finalize-document-upload/index.ts:9-13`)، تعارض صريح مع `docs/rls-matrix.md:8-14`؛ **باقة `business-assets` عامة وتسمح بـ SVG** — XSS مخزّن محتمل (0004:19).
- [متوسط] دوال 0012 بلا `set search_path`؛ دوال عامة جديدة بلا revoke من PUBLIC/anon؛ منح EXECUTE على `private.command_*` لـ authenticated يتجاوز طبقة الـ wrapper (0003:68-83)؛ تعارض صلاحيات cashier مع المصفوفة الموثقة (0003:132-134 مقابل `rls-matrix.md:22`)؛ سياسات قديمة لا تتحقق من `link_status` (0003:147-152)؛ `upload_sessions` بلا revoke ولا policy (0006:79-93,522-523)؛ سياسة `customer_currency_balances` تقارن هوية خاطئة (0012:42)؛ بقايا تطوير في `config.toml:48,50-61`.

**التقرير التفصيلي:** `analysis-reports/03-security-rls.md` (حرج 5 · عالي 3 · متوسط 8 · منخفض 4 · إيجابي 10).

### 3.4 دوال Edge التسع — التقرير 04

**الخلاصة:** البنية المشتركة `_shared` رصينة (تحقق JWT فعلي، فصل صلاحيات least-privilege، تشفير هواتف صحيح، rate limiting ذرّي مركزي، معالج طابور متين). الكسور تتركز في `send-whatsapp-otp` (أسرار + بلا حماية) وIDOR في `generate-statement` وأخطاء منطقية في عمال الخلفية.

**أهم الملاحظات:**
- [حرج] أسرار مرمّزة في الكود (X-2)؛ `send-whatsapp-otp` بلا مصادقة وكسر نموذج OTP (X-2/X-4)؛ **IDOR في `generate-statement`:** عند تمرير `statementId` جاهز يُجلب الكشف عبر service client بلا أي تحقق ملكية (سطور 425، 449-457) ويُعاد مع الأرصدة ورمز التحقق ورابط PDF موقّع (534-552) — كشف كامل للبيانات المالية لأي مستخدم مصادق.
- [عالي] تجاوز rate limiting في `verify-statement` عبر ترويسة `x-forwarded-for` مزوّرة (سطور 13-14) مع رمز تحقق 48 بت قابل للتعداد؛ `p_key_version` ثابتة =1 في `customer-directory:35` تكسر تدوير مفاتيح التشفير؛ تكرار الإشعارات عند فشل توكن جزئي في `process-notification-outbox:169-216`.
- [متوسط] حساب تواريخ الأتمتة بـ UTC بدل timezone النشاط — انزياح يوم كامل في التذكيرات (`process-automation-rules:83,114-120,143`)؛ نافذة الإرسال العابرة لمنتصف الليل لا تعمل إطلاقاً (56-58)؛ تخويل الرفع على صلاحية القراءة (مشترك مع 03)؛ لا فحص magic bytes للملفات (`finalize-document-upload:30-33`)؛ عمود الرصيد الجاري في PDF يفقد الإشارة السالبة بـ `Math.abs` (`generate-statement:45-51,337`)؛ مدخلات مالية/زمنية بلا تحقق نطاق.

**التقرير التفصيلي:** `analysis-reports/04-edge-functions.md` (حرج 3 · عالي 3 · متوسط 6 · منخفض 7 · إيجابي 8).

### 3.5 المحاسبة بالقيد المزدوج — التقرير 05

**الخلاصة:** النواة المحاسبية (009–011) **سليمة رياضياً ومحاسبياً وتستحق الثقة**: تحقق يدوي من كل ترحيل (افتتاحي/دين/سداد/خصم/عكس) — كلها متوازنة وصحيحة، والتوازن مفروض بـ constraint trigger مؤجل على مستوى قاعدة البيانات (0010:204-221)، والذرّية كاملة (مستحيل قيد تشغيلي بلا يومية). لكن هذا البناء محاصر بـ 0012 وعقد API المكسورين وفجوة اختبارات.

**أهم الملاحظات:**
- [حرج] 0012 مكسور ويهدد سلامة المحاسبة (X-1) — ومحاسبياً يُدخل تعدد عملات دون أي تعديل على طبقة القيد المزدوج: `journal_entry_lines` بلا عمود عملة وميزان المراجعة سيجمع عملات مختلفة في حساب واحد؛ عقد API غير المتطابق يمنع وصول بيانات التطبيق أصلاً (X-3).
- [عالي] قبول النزاع جزئياً على قيد خصم يفشل دائماً (0002:586-589 + 0010:356)؛ وصف القيد اليومي قد يتجاوز 500 حرف ويسقط العملية كلها (0010:260 — بادئة تصل 30 حرفاً + وصف حتى 500 مقابل حد 500 في 0010:68)؛ تغطية الاختبارات مسار سعيد فقط — العكس وقفل الفترة والرفض السلبي بلا اختبار.
- [متوسط] مصدران للحقيقة في رصيد العميل بلا آلية تسوية (0010:479-490)؛ إيصالات الأوامر غير مكتملة للقيود اليدوية والعكسيات (0011:5-10).

**التقرير التفصيلي:** `analysis-reports/05-double-entry.md` (حرج 2 · عالي 3 · متوسط 2 · منخفض 4 · إيجابي 11).

### 3.6 تعدد العملات — التقرير 06

**الخلاصة:** الميزة **غير قابلة للتشغيل إطلاقاً بحالتها الحالية** — «حرجة بامتياز». الـ migration لن ينجح، والتريجر والدالة يستخدمان قيماً غير موجودة في الـ enums، والموبايل يستدعي تواقيع لا تطابق أي دالة منشورة، وتريجر التحقق الأصلي يمنع أي عملة مخالفة لعملة المحل. لا يوجد أي تحويل بين العملات (لا أسعار صرف)، والتصميم يفترض فصلاً كاملاً بين العملات لكن الشاشات والـ views والمحاسبة تجمع المبالغ بلا تمييز عملة في مواضع عدة.

**أهم الملاحظات:**
- [حرج] كلها في X-1 وX-3 (الـ migration غير قابل للتطبيق، تريجر enum، دالة فاشلة منطقياً، عدم تطابق تواقيع RPC، التناقض المعماري مع حارس العملة الواحدة 0010:329-330).
- [عالي] **الـ views ودوال الكشوفات تجمع عملات مختلفة بلا تجميع:** `business_customer_balances` (0002:653-670) و`business_customer_account_positions` (0010:479-490) و`command_generate_statement` (0008:152-164) — «500 ر.ي + 100 $ = 600 ر.ي» في كشوفات موقعة قانونياً؛ `journal_entry_lines` بلا عملة (0010:76-88)؛ أرصدة العملات المحلية في الموبايل لا تُزامَن من السيرفر إطلاقاً والتحديث التفاؤلي يتجاهل `reversal/opening_balance` (`sync_engine.dart:187-193`، `app_database.dart:405-411`)؛ إجماليات لوحة التاجر تجمع عملات مختلفة معروضة برمز واحد (`merchant_controller.dart:122-124`، `merchant_home_screen.dart:447-456`).
- [متوسط] رموز `'ر.ي'` ثابتة في شاشات تعرض عملات أخرى (5 مواقع)؛ لا أسعار صرف وعملة المحل مثبتة `'YER'` برمجياً في 4 مواضع (`auth_controller.dart:164,262,453`، `merchant_controller.dart:98`)؛ التصنيفات نصوص حرة بلا قيود؛ إشعارات الأتمتة تستخدم عملة المحل بدل عملة القيد (`process-automation-rules:207,235`).

**التقرير التفصيلي:** `analysis-reports/06-multi-currency.md` (حرج 5 · عالي 5 · متوسط 4 · منخفض 3 · إيجابي 7).

### 3.7 طبقة بيانات الموبايل (Flutter) — التقرير 07

**الخلاصة:** البنية المعمارية (كتابة تفاؤلية + طابور أوامر + RPC فقط) صحيحة نظرياً، لكن التنفيذ فيه **كسر كامل في عقد الاتصال** (X-3) ومسارات التفاف كاملة في المصادقة (X-4). النتيجة: انحراف دائم وصامت بين أرصدة العميل المعروضة وقاعدة البيانات المركزية — «أسوأ سيناريو ممكن لمنتج مالي».

**أهم الملاحظات:**
- [حرج] عدم تطابق RPC (X-3)؛ أربع عمليات تستدعي دوالاً غير موجودة (X-3)؛ OTP على العميل مع أكواد التفاف (X-4)؛ تسجيل دخول بلا بيانات اعتماد (X-4)؛ استعادة جلسة بلا توكن (X-4)؛ مفاتيح وأسرار مضمّنة في كود العميل (X-2 + `supabase_config.dart:8-13` عنوان `127.0.0.1:55321` ومفتاح anon محلي افتراضي، وعنوان Supabase قابل للتعديل من المستخدم عبر SharedPreferences بلا تحقق — سطور 36-49).
- [عالي] حساب الأرصدة في العميل يتجاهل `reversal` — رصيد خاطئ فوراً بعد أي عكس (`app_database.dart:405-411`)؛ **إضافة عميل جديد لا تُدرج أي أمر في طابور المزامنة** — العميل لا يصل للسيرفر أبداً وكل قيوده ميتة (`merchant_repository.dart:46-81`)؛ السحب لا يجلب `ledger_entries` إطلاقاً ويكتب فوقياً بلا حل تعارضات (`sync_engine.dart:147-220`)؛ كتابة مباشرة على `profiles` (خرق API-only — `auth_controller.dart:230,416`)؛ **حذف طابور العمليات غير المرسلة عند تسجيل الخروج** — فقدان بيانات مالية بصمت (`auth_controller.dart:505` + `app_database.dart:511-520`)؛ لا حد أقصى لإعادة المحاولة ولا dead-letter؛ `occurred_at` من ساعة الجهاز القابلة للتعديل؛ التخزين المحلي غير مشفر (SQLite بلا SQLCipher + `pendingPassword` نص صريح في الذاكرة)؛ معرّفات محلية `'entry-/cust-/biz-'` لا تُسوَّى مع معرّفات السيرفر.
- [متوسط] N+1 في سحب الأرصدة؛ عملة العكس الافتراضية 'YER' خاطئة (`merchant_repository.dart:232`)؛ ابتلاع أخطاء وإرجاع بيانات قديمة كأنها حية؛ ازدواجية مصادر OTP؛ `offlineMockMode` قابل للتفعيل من المستخدم (`supabase_config.dart:17,29,39-48`)؛ مسارات أوامر ميتة في المزامنة؛ ترحيل SQLite هش بـ try/catch صامتة.

**التقرير التفصيلي:** `analysis-reports/07-mobile-data-layer.md` (حرج 6 · عالي 9 · متوسط 7 · منخفض 5 · إيجابي 6).

### 3.8 عقود API بين الواجهات والباكند — التقرير 08

**الخلاصة:** الفجوة **جذرية وليست هامشية** — «التطبيق أقرب إلى نموذج عرض (demo) يعمل على SQLite محلي؛ التكامل الفعلي مع الباكند شبه معدوم». 15 ملاحظة حرجة توثق أن غالبية العمليات غير موصولة: مصفوفة التطابق النهائي (§9 في التقرير) تُظهر 20 عملية، منها 17 بحكم ❌ (غير موصول/معطل) و2 ⚪ (ميزات بلا واجهة مقبولة للـ MVP) وواحدة فقط سليمة الاتجاه.

**أهم الملاحظات (إضافة لما في X-2/X-3/X-4):**
- [حرج] **شاشة العميل بأكملها بيانات وهمية ثابتة** — رصيد ثابت `'450,000 ر.ي'` وزر «تأكيد السجل» يعرض نجاحاً بلا أي استدعاء (`customer_home_screen.dart:94,181-195,264-272`) — كل تجربة العميل (تأكيد القيود، الرد على الربط، النزاعات، الكشف المجمع) غير منفذة؛ `profiles` بأعمدة وهمية وبلا منحة INSERT (C-11)؛ `getMembers`/`getDisputes`/`getDisputeMessages` باستعلامات مكسورة (C-12/13/14)؛ **المرفقات لا تُرفع إلى Storage إطلاقاً** — «سند موثق بالمرفقات» غير موجود فعلياً (C-15).
- [عالي] **صفر استدعاءات لكل Edge Functions التسع** (`functions.invoke` غير موجودة في كل `mobile/lib`) — لا PDF، لا تشفير هاتف، لا رفع مرفقات؛ تجاهل بنية الأوفلاين الخلفية (`command_receipts`/`sync_checkpoints`/`device_installations` من 0006)؛ مسارات ميتة في محرك المزامنة (`confirm_ledger_entry`/`open_dispute` لا يُدرجهما أحد في الطابور)؛ عمليات النزاع أونلاين فقط وتفشل بصمت.
- [متوسط] `full_schema.sql` بتعريفات مكررة متضاربة؛ `phone` غير موجود في `business_customers`؛ نوع `'fee'` الشبح؛ N+1؛ `sync_status` لا يُحدَّث بعد النجاح؛ سقوط `loginWithPassword` للكاش الوهمي حتى عند نجاح الدخول الفعلي (بسبب عمود `user_type` الوهمي).

**التقرير التفصيلي:** `analysis-reports/08-api-contracts.md` (حرج 15 · عالي 7 · متوسط 7 · منخفض 3 · إيجابي 4).

### 3.9 التوثيق مقابل التنفيذ — التقرير 09

**الخلاصة:** 28 ملاحظة (3 حرجة) تكشف انفصالاً بين الوثائق والواقع في الاتجاهين: ميزات منفذة توصف كغير موجودة، وميزات موصوفة كمنفذة وهي مكسورة، وترحيل كامل (0012) خارج كل الوثائق. المطابقات الإيجابية واسعة أيضاً: كل RPCs الموثقة في مرجع API منفذة بنفس الأسماء والمعاملات والأدوار (P-1)، والمبادئ المالية غير القابلة للتنازل منفذة كما وُثقت (P-2).

**أهم الملاحظات:**
- [حرج] 0012 غير موثق ومكسور (X-1)؛ **`process-automation-rules` موثق كعامل جاهز لكنه مكسور وقت التشغيل في مساريه:** إدراج في `reminders` بأعمدة غير موجودة (`ledger_entry_id/title/body` — الجدول الفعلي يتطلب `sent_by_user_id` و`message_snapshot`، 0001:326-338)، استعلام `disputes.status` غير الموجود (الحالة في `dispute_state`)، وإشعارات تُدرج مباشرة متجاوزة `enqueue_notification` فلا يصل أي Push إطلاقاً (`index.ts:213-221,274,225-237`)؛ `send-whatsapp-otp` غير موثقة بأسرار مضمّنة وبلا حماية (X-2).
- [عالي] وثيقتان مرجعيتان تدّعيان أن `generate-statement` يعيد 501 غير منفذ بينما هو منفذ بالكامل (562 سطراً) — قرار إطلاق مبني عليهما خاطئ (`api-reference-and-operational-flow.md:127`، `database-architecture-and-operations-spec.md:21,290`)؛ الوثائق تدّعي `process-automation-rules` معطّل بينما هو «مفعّل ظاهرياً وفاشل فعلياً» — أخطر من المعطّل؛ **PDF كشف الحساب إنجليزي فقط بخط Helvetica لا يدعم العربية** — وصف القيد العربي سيظهر مشوهاً في وثيقة مالية موقعة، مخالفاً وعد «العربية أولاً» (`generate-statement:65-66,86,235-241`)؛ `cleanup-expired-data` موثقة وغير موجودة (مع `upload_sessions` المنتهية بلا منظّف).
- [متوسط] جداول موثقة كمطلوبة وغير منفذة (`dispute_sla`/`security_events`/`business_settings`)؛ 0012 يخالف مبدأ «الرصيد يُشتق ولا يُخزن» وقرار «عملة واحدة لكل محل» الموثقين؛ تناقضات داخلية في الوثائق (حدود الترحيلات، أسماء الملفات، قيمة `discount` الناقصة، سياسة الدفع الأكبر من الرصيد)؛ `First-version.md` يقترح NestJS/Laravel بينما التنفيذ Supabase؛ `edge-functions.md` يسرد 6 من 9؛ قوائم RPC ناقصة/زائدة بين الوثائق.

**التقرير التفصيلي:** `analysis-reports/09-docs-vs-implementation.md` (حرج 3 · عالي 4 · متوسط 10 · منخفض 4 · إيجابي 7).

### 3.10 الاختبارات وجاهزية الإطلاق — التقرير 10

**الخلاصة:** 31 تأكيد pgTAP جيدة الصنع (حتمية، داخل معاملة بـ rollback، تحاكي مستخدمين حقيقيين تحت RLS) تغطي المسار السعيد بشكل جيد — لكن لا CI، لا اختبارات نفي، لا اختبارات موبايل حقيقية، سر مرتكب في الكود، مسار مصادقة إنتاجي غير محسوم، ولا نسخاً احتياطياً أو مراقبة. تقدير التقرير: جاهزية «تجريبي مغلق» تحتاج **2–3 أسابيع عمل مركّز** على البنود 1–8 من قائمة الفجوات.

**أهم الملاحظات:**
- [حرج] لا اختبارات RLS سلبية إطلاقاً (`001_schema_contract.sql:11-13` يتحقق من *وجود* السياسات لا فعاليتها)؛ لا اختبارات للـ Edge Functions التسع؛ اختبارات موبايل شبه معدومة (`widget_test.dart` من 15 سطراً — أخطر منطق، طابور الأوفلاين، بلا اختبار)؛ لا CI إطلاقاً؛ سر OpenWA مثبت في الكود (X-2).
- [عالي] تعدد العملات غير مختبر؛ لا اختبارات تزامن؛ عدم القابلية للتعديل غير مختبرة مباشرة؛ **تناقض سياسة الدفع الأكبر من الرصيد** — `First-version.md:1037-1043` يقرر المنع بينما التنفيذ يسمح رصيداً دائناً (`0010:39` + الاختبار `003:33` يختبر دفعة 230 مقابل رصيد 180 كسلفة)؛ سيناريوهات النزاعات ناقصة؛ صلاحيات الأدوار غير مختبرة سلبياً؛ مسار OTP الإنتاجي غير محسوم (`config.toml:55,59-61`)؛ لا استراتيجية نسخ احتياطي/استعادة موثقة رغم كونها ميزة معلنة (`First-version.md:102,1380`)؛ لا مراقبة إطلاقاً (لا Sentry/Crashlytics في `pubspec.yaml`).
- [متوسط] قيود المبالغ غير مختبرة؛ لا اختبار لـ `verify-statement` العام؛ `seed.sql` فارغ عملياً (سيعيق المرحلة التجريبية المغلقة)؛ لا اختبارات أتمتة/إشعارات ولا أداء؛ `mobile/README.md` ما زال قالب Flutter الافتراضي.

**التقرير التفصيلي:** `analysis-reports/10-tests-launch-readiness.md` (حرج 5 · عالي 7 · متوسط 6 · منخفض 2 · إيجابي 5).

---

## 4. مصفوفة المخاطر الموحدة (كل الملاحظات الحرجة والعالية)

الملاحظات المكررة عبر عدة تقارير دُمجت في صف واحد مع تجميع كل مصادرها. عمود «المصدر» يشير إلى رقم التقرير والملف:السطر كما ورد فيه. الجهد تقديري (س = ساعات، ي = يوم عمل).

### 4.1 الملاحظات الحرجة (26 بنداً موحداً من 52 ملاحظة أصلية)

| # | الوصف | المصدر (تقرير — ملف:سطر) | الجهد |
|---|---|---|---|
| K1 | الترحيل 0012 غير قابل للتطبيق: سياسة RLS تستدعي `has_business_permission`/`business_permission` غير الموجودين + بلا `begin/commit` | 01/02/03/05/06/08/09 — `202608180012:38-43` | 1–2 ي (ضمن إعادة كتابة 0012) |
| K2 | تريجر 0012 يقارن enum بـ `'in'/'out'` → تعطيل كل إدراج قيد مالي في النظام | 01/02/03/05/06/08/09 — `0012:51-111` (خاصة 59) | س (ضمن K1) |
| K3 | FK خاطئ: `customer_currency_balances.customer_id → profiles(id)` بدل `customers(id)` | 01/02/03/05/08/09 — `0012:17` مقابل `0001:162` | س (ضمن K1) |
| K4 | دالة عامة `public.command_create_ledger_entry` بلا فحص تفويض وبأخطاء نوع متعددة — كتابة عابرة للمستأجرين ومسار موازٍ بلا ضوابط | 01/02/03/05/06/09 — `0012:114-209` | س–1 ي (ضمن K1) |
| K5 | تناقض بنيوي 0010↔0012: قيد CHECK يفرض `debit/credit` والدالة تدرج `'in'/'out'` و`'fee'` | 01/02/03/06/09 — `0010:314-319` مقابل `0012:157-163` | س (ضمن K1) |
| K6 | مفتاح OpenWA وجلسة مضمّنان نصياً في Edge Function ومكرران في كود الموبايل (APK/IPA) | 03/04/07/08/09/10 — `send-whatsapp-otp/index.ts:4-5`؛ `whatsapp_otp_service.dart:28-29` | س (تدوير فوري) + س (تنظيف الكود) |
| K7 | OTP يُولَّد ويُخزَّن ويُتحقق على العميل + كودا تجاوز ثابتان `123456/000000` + قبول `mockOtpCode` | 03/04/07/08 — `whatsapp_otp_service.dart:95-99,188-241,196-199`؛ `auth_controller.dart:36,315-324,397` | 2–3 ي (نقل server-side) |
| K8 | `send-whatsapp-otp` بلا مصادقة ولا rate limit وCORS `*` — إرسال واتساب اعتباطي باسم النشاط | 03/04/09/10 — `send-whatsapp-otp/index.ts:13-79` | 1 ي |
| K9 | IDOR في `generate-statement`: جلب أي كشف بمعرّفه عبر service client بلا تحقق ملكية + إرجاع رابط PDF موقّع | 04 — `generate-statement/index.ts:425,449-457,534-552` | س |
| K10 | حمولة `create_ledger_entry` من الموبايل بستة معاملات غير موجودة في التوقيع → فشل `PGRST202` دائم لكل قيد | 02/05/06/07/08 — `merchant_repository.dart:129-143`؛ `sync_engine.dart:123`؛ `0002:413-425` | 2–3 ي (توحيد العقد) |
| K11 | أربع عمليات تستدعي RPCs بأسماء غير موجودة (`command_add_dispute_message/resolve_dispute/invite_business_member/generate_statement`) + معاملات خاطئة | 02/07/08 — `merchant_repository.dart:349,367,422,452` | 1 ي |
| K12 | `reverse_ledger_entry` بمعامل `p_original_entry_id` بدل `p_entry_id`، و`apply_customer_discount` بمعامل زائد `p_currency_code` | 06/07/08 — `merchant_repository.dart:190-196,259-263` | س |
| K13 | إضافة عميل جديد لا تصل الباكند إطلاقاً (لا RPC ولا إدراج في الطابور) → سلسلة كسر: عميل → قيود → أرصدة → كشوفات | 07/08 — `merchant_repository.dart:46-81` | 1–2 ي |
| K14 | تسجيل دخول بديل بلا بيانات اعتماد من الكاش المحلي + هويات وهمية `usr-/biz-` غير موجودة في `auth.users` | 07/08 — `auth_controller.dart:104,128-138,155,238-289,444` | 1–2 ي |
| K15 | استعادة الجلسة من SharedPreferences بلا توكن ولا تحقق — انتحال هوية trivial | 07 — `auth_controller.dart:71-94` | س |
| K16 | شاشة العميل بأكملها بيانات وهمية ثابتة — تأكيد القيود/الربط/النزاعات/الكشف المجمع غير منفذة | 08 — `customer_home_screen.dart:93-104,181-195,264-272` | 3–5 ي |
| K17 | قراءة/كتابة `profiles` بعمودي `user_type/phone` غير الموجودين وبلا منحة INSERT | 08 — `auth_controller.dart:118-119,230-235,416-421` | س |
| K18 | `getMembers` يقرأ أعمدة `member_role/permissions/is_active` غير الموجودة → شاشة الفريق فارغة دائماً | 08 — `merchant_repository.dart:385-391` | س |
| K19 | `getDisputes` بتضمين `profiles!customer_id` غير الصالح → قائمة النزاعات لا تعمل أونلاين | 08 — `merchant_repository.dart:295-300` | س |
| K20 | `getDisputeMessages` بتضمين `profiles` بلا FK → محادثة النزاع فارغة دائماً | 08 — `merchant_repository.dart:333-336` | س |
| K21 | المرفقات لا تُرفع إلى Storage إطلاقاً — «سند موثق بالمرفقات» غير موجود فعلياً | 08 — `attachment_picker_widget.dart`؛ `create_ledger_entry_sheet.dart:84` | 1–2 ي |
| K22 | تناقض معماري: واجهة تعدد عملات (YER/SAR/USD) مقابل حارس العملة الواحدة `validate_ledger_entry_insert` | 01/02/06/08 — `0010:329-330`؛ `create_ledger_entry_sheet.dart:43-47,90` | قرار + 3–5 ي |
| K23 | `process-automation-rules` مكسور وقت التشغيل: أعمدة `reminders` غير موجودة، `disputes.status` وهمي، إشعارات تتجاوز outbox فلا Push إطلاقاً | 09 — `process-automation-rules/index.ts:213-221,274,225-237` | 1–2 ي |
| K24 | لا اختبارات RLS سلبية إطلاقاً (عزل المستأجرين جوهر المنتج) | 10 — `tests/database/001_schema_contract.sql:11-13`؛ `README.md:94-103` | 2–3 ي |
| K25 | لا اختبارات لأي من Edge Functions التسع، واختبار موبايل وحيد smoke من 15 سطراً مقابل 53 ملف Dart | 10 — `supabase/functions/` (لا `*test*`)؛ `mobile/test/widget_test.dart` | 3–5 ي |
| K26 | لا CI إطلاقاً — لا بوابة جودة ولا lint ولا تشغيل اختبارات عند الدمج | 10 — لا `.github/workflows`؛ `README.md:44-49` | 1 ي |

### 4.2 الملاحظات العالية (46 بنداً موحداً من 51 ملاحظة أصلية)

| # | الوصف | المصدر (تقرير — ملف:سطر) | الجهد |
|---|---|---|---|
| H1 | تسابق العكس مقابل الإنشاء: قفلان مختلفان يسمحان برصيد سالب (−100) رغم نجاح كل فحص على حدة | 02 — `0006:267` مقابل `0006:314` | 1 ي |
| H2 | idempotency معلن لثمانية أوامر ومنفذ لثلاثة؛ `post_manual_journal` بلا `client_request_id` أصلاً | 02/05 — `0011:5-10`؛ `0010:410-436` | 1–2 ي |
| H3 | كشف الحساب ليس لقطة متسقة (ثلاثة استعلامات بلا قفل) وبلا idempotency — كشف «موقّع» قد لا يطابق بنوده | 02 — `0008:152-180` | 1 ي |
| H4 | دوال أُنشئت بعد migration الصلاحيات 0003 تحتفظ بمنح `EXECUTE TO PUBLIC` الافتراضي | 02/03 — `0003:33` وكل migrations اللاحقة | 1 ي |
| H5 | حذف CASCADE في `customer_currency_balances` يكسر سياسة RESTRICT المعتمدة في الجداول المالية | 01 — `0012:15-16` | س |
| H6 | حالة `voided` لليوميات غير قابلة للوصول (تريجر يمنع أي UPDATE) وميزان المراجعة لا يرشّح بالحالة | 01/02/05 — `0010:10,121-124,465-477` | س |
| H7 | رصيد مشتق `customer_currency_balances` بلا حماية تعديل ولا وظيفة مصالحة — انحراف صامت إلى الأبد | 01 — `0012:13-26` | 1 ي |
| H8 | سياسة `service_role` ميتة (يتجاوز RLS أصلاً) توحي بحماية زائفة ولا تترك مسار تصحيح للأرصدة | 01 — `0012:45-48` | س |
| H9 | تخويل رفع الملفات مبني على صلاحية القراءة ثم كتابة بـ service_role — العميل read-only وviewer يرفقان ملفات بأي قيد | 03/04 — `signed-document-upload/index.ts:10-14`؛ `finalize-document-upload/index.ts:9-13` | 1 ي |
| H10 | باقة `business-assets` عامة وتسمح بـ `image/svg+xml` — XSS مخزّن محتمل | 03 — `202607120004:19` | س |
| H11 | تجاوز rate limiting في `verify-statement` عبر ترويسة `x-forwarded-for` مزوّرة + تعداد رموز 48-بت يكشف أرصدة | 04 — `verify-statement/index.ts:13-14` | س |
| H12 | `p_key_version` ثابتة =1 في `customer-directory` تكسر تدوير مفاتيح تشفير الهواتف | 04 — `customer-directory/index.ts:35` | س |
| H13 | فشل توكن جزئي في outbox يفشّل الإشعار كله → إعادة إرسال مكررة على أجهزة المستخدم | 04 — `process-notification-outbox/index.ts:169-216` | س |
| H14 | قبول نزاع `partially_accepted` على قيد خصم يفشل دائماً — لا مسار إغلاق نظيف | 02/05 — `0006:463-468`؛ `0010:356` | س |
| H15 | وصف القيد اليومي (بادئة + وصف القيد) قد يتجاوز 500 حرف → سقوط العملية كلها | 05 — `0010:260` مقابل `0010:68` و`0001:167` | س |
| H16 | اختبارات المحاسبة مسار سعيد فقط: العكس/قفل الفترة/الرفض السلبي/عدم القابلية للتعديل بلا اختبار | 05 — `tests/database/003_double_entry_accounting.sql` | 1–2 ي |
| H17 | الـ views ودوال الكشوفات تجمع مبالغ بعملات مختلفة بلا `group by currency_code` — مجاميع بلا معنى في كشوفات موقعة | 06 — `0002:653-670`؛ `0010:479-490`؛ `0008:152-164` | 1–2 ي |
| H18 | `journal_entry_lines` بلا عمود عملة — دفتر الأستاذ وميزان المراجعة يجمعان عملات مختلفة في حساب واحد | 01/06 — `0010:76-88` | 1–2 ي |
| H19 | أرصدة العملات المحلية لا تُزامَن من السيرفر إطلاقاً والتحديث التفاؤلي يتجاهل `reversal/opening_balance` | 06/07 — `sync_engine.dart:187-193`؛ `app_database.dart:405-411` | 1–2 ي |
| H20 | إجماليات لوحة التاجر تجمع عملات مختلفة معروضة برمز عملة واحد — تضليل مالي مباشر | 06 — `merchant_controller.dart:122-124`؛ `merchant_home_screen.dart:447-456` | س |
| H21 | السحب (Pull) لا يجلب `ledger_entries`/النزاعات/الكشوفات + كتابة فوقية بلا حل تعارضات — جهاز ثانٍ لن يرى أي قيد | 07/08 — `sync_engine.dart:147-220`؛ `app_database.dart:303-310` | 2–3 ي |
| H22 | كتابة مباشرة `profiles.upsert` من العميل — خرق لمبدأ API-only | 07 — `auth_controller.dart:230,416` | س |
| H23 | حذف طابور العمليات غير المرسلة عند تسجيل الخروج — فقدان بيانات مالية بصمت بلا تحذير | 07 — `auth_controller.dart:505`؛ `app_database.dart:511-520` | س |
| H24 | لا حد أقصى لإعادة المحاولة ولا dead-letter ولا تمييز أخطاء 4xx الدائمة — طابور يعلق للأبد | 07/08 — `app_database.dart:496-503`؛ `sync_engine.dart:116-143` | 1 ي |
| H25 | `occurred_at` من ساعة الجهاز القابلة للتعديل — تلاعب بتواريخ القيود والاستحقاق | 07 — `merchant_repository.dart:101,141,168,236` | س |
| H26 | التخزين المحلي غير مشفر: SQLite بلا SQLCipher + أسرار في SharedPreferences + `pendingPassword` نص صريح | 07 — `app_database.dart:11`؛ `supabase_config.dart:45-48`؛ `auth_controller.dart:22,56` | 1–2 ي |
| H27 | معرّفات محلية مؤقتة (`entry-/cust-/biz-`) لا تُسوَّى مع معرّفات السيرفر بعد النجاح | 07 — `merchant_repository.dart:54,100`؛ `auth_controller.dart:155` | 1–2 ي |
| H28 | صفر استدعاءات لكل Edge Functions التسع من الموبايل — لا PDF، لا تشفير هاتف، لا رفع مرفقات، لا تحقق كشوف | 08 — `functions.invoke` = 0 نتائج في `mobile/lib` | 2–3 ي |
| H29 | قيمة `p_scope: 'customer'` خاطئة — الـ enum يقبل `business_customer/customer_consolidated` فقط | 08 — `merchant_repository.dart:453`؛ `0001:31` | س |
| H30 | `inviteMember` يرسل `p_phone` في خانة `p_target_user_id uuid` — خلط مفاهيمي كامل | 08 — `merchant_repository.dart:422-426`؛ `invite_member_sheet.dart:114-132` | س |
| H31 | تجاهل بنية الأوفلاين الخلفية: لا تسجيل جهاز، لا checkpoints، لا قراءة إيصالات | 08 — `0006:5-39`؛ `0011:5-10` | 1–2 ي |
| H32 | مسارات ميتة في محرك المزامنة: `confirm_ledger_entry`/`open_dispute` لا يُدرجهما أي كود في الطابور | 07/08 — `sync_engine.dart:128-132` | 1 ي |
| H33 | عمليات النزاع أونلاين فقط بلا طابور وتفشل بصمت برسالة مضللة «تحقق من اتصالك» | 08 — `merchant_repository.dart:346-377` | 1 ي |
| H34 | وثيقتان مرجعيتان تدّعيان `generate-statement` غير منفذ (501) بينما هو منفذ بالكامل — قرار إطلاق خاطئ الاتجاهين | 09 — `api-reference-and-operational-flow.md:127`؛ `database-architecture-and-operations-spec.md:21,290` | س |
| H35 | الوثائق تدّعي `process-automation-rules` معطّل بينما هو «مفعّل ظاهرياً وفاشل فعلياً» — ثقة زائفة | 09 — `api-reference-and-operational-flow.md:126` | س |
| H36 | PDF كشف الحساب إنجليزي فقط بخط Helvetica — لا عربية ولا RTL في وثيقة مالية موقعة لسوق عربي | 09 — `generate-statement/index.ts:65-66,86,235-241` | 2–3 ي |
| H37 | `cleanup-expired-data` موثقة وغير موجودة + `upload_sessions` المنتهية بلا منظّف | 09 — `first-version-product-architecture-spec.md:404`؛ `0006:128` | 1 ي |
| H38 | تعدد العملات غير مختبر إطلاقاً (لا نتيجة لبحث `currency` في الاختبارات) | 06/10 — `supabase/tests/database/` | 1–2 ي |
| H39 | لا اختبارات تزامن رغم طلبها صراحة في المواصفة | 10 — `First-version.md:1536` | 1–2 ي |
| H40 | عدم القابلية للتعديل/الحذف ومنع عكس العكس غير مختبرة مباشرة | 10 — `README.md:99` | 1 ي |
| H41 | تناقض سياسة «الدفع الأكبر من الرصيد»: الوثائق تقول المنع والتنفيذ يسمح رصيداً دائناً افتراضياً | 02/09/10 — `First-version.md:1037-1043`؛ `0010:39,366-367`؛ `003:33` | 1 ي (قرار + توحيد) |
| H42 | سيناريوهات النزاعات ناقصة (رفض، منع تكرار، تصعيد) | 10 — `README.md:102`؛ `First-version.md:782` | 1–2 ي |
| H43 | صلاحيات الأدوار غير مختبرة سلبياً (cashier، تأكيد نيابة، عميل غير مرتبط) | 10 — `First-version.md:1011` | 1–2 ي |
| H44 | مسار المصادقة الإنتاجي غير محسوم: SMS معطل + بريد معطل + اعتماد على OpenWA يشير localhost | 10 — `config.toml:55,59-61`؛ `send-whatsapp-otp/index.ts:3` | 2–3 ي |
| H45 | لا استراتيجية نسخ احتياطي/استعادة موثقة رغم كونها ميزة معلنة ومتطلب اعتمادية | 10 — `First-version.md:102,1380` | 1 ي |
| H46 | لا مراقبة إطلاقاً: لا Sentry/Crashlytics ولا تنبيهات للدوال وcron | 10 — `First-version.md:1717-1722`؛ `pubspec.yaml` | 1–2 ي |

---

## 5. خارطة طريق الإطلاق

خطة إصلاح على ثلاث مراحل، مبنية من توصيات الأولوية في التقارير العشرة (خاصة قوائم «توصيات الإصلاح» في 01/03/04/06/07/08/09 و«قائمة فجوات الإطلاق» في 10). تقدير تقرير 10 المرجعي: جاهزية «تجريبي مغلق» = 2–3 أسابيع عمل مركّز.

### المرحلة صفر — إصلاحات تحظر الإطلاق (لا يجوز نشر أي شيء قبلها)

**الهدف:** نظام قابل للنشر، يكتب مالياً من التطبيق فعلاً، وبلا أسرار مسربة ولا التفاف مصادقة.
**الجهد التقديري:** ~2–3 أسابيع (فريق 2–3 مهندسين).

| # | البند | البنود المرتبطة |
|---|---|---|
| 0.1 | **تدوير مفتاح OpenWA فوراً** وإزالة كل القيم الافتراضية من `send-whatsapp-otp/index.ts:4-5` و`whatsapp_otp_service.dart:28-29` + فحص أسرار شامل للمستودع | K6, K8 |
| 0.2 | **حسم الترحيل 0012:** حذفه أو إعادة كتابته من الصفر (تعريف/استبدال `has_business_permission`، تصحيح الاتجاهات إلى `debit/credit`، FK إلى `customers(id)`، فحص أدوار + revoke + `search_path`، إزالة `'fee'`) — مع حسم القرار المعماري: عملة واحدة لكل محل (وإزالة محدد العملات من الموبايل) أو تعدد حقيقي (وتحديث 0010 والمحاسبة والـ views) | K1–K5, K22 |
| 0.3 | **توحيد عقد RPC:** تصحيح أسماء الدوال الأربع، توحيد توقيع `create_ledger_entry`، تصحيح `p_entry_id` و`p_scope` و`p_target_user_id`، وإزالة/إضافة `p_currency_code` في الخصم — ثم اختبار تكامل end-to-end إلزامي لكل أمر | K10–K12, H29, H30 |
| 0.4 | **نقل OTP إلى الخادم بالكامل** (توليد/تخزين/تحقق في Edge Function + جدول private بمهلة ومحاولات محدودة) وحذف كودي `123456/000000` وقبول `mockOtpCode` | K7, K8 |
| 0.5 | **إزالة مسارات الالتفاف في المصادقة:** منع الدخول دون جلسة Supabase صالحة، حذف fallback الكاش المحلي، ومنع استعادة الجلسة من SharedPreferences بلا توكن | K14, K15 |
| 0.6 | **إصلاح IDOR في `generate-statement`:** تحقق ملكية الكشف عبر عميل المستخدم (RLS) قبل الجلب بمعرّف | K9 |
| 0.7 | **ربط إضافة العميل بالباكند:** استدعاء `add_business_customer` + إدراج الأمر في طابور المزامنة + تسوية المعرّفات المحلية بمعرّفات السيرفر | K13, H27 |
| 0.8 | **إصلاح الاستعلامات المكسورة:** أعمدة `business_members` الفعلية، تضمينات النزاعات وفق FKs الحقيقية، إسقاط `user_type/phone` من `profiles` | K17–K20 |
| 0.9 | **إيقاف ابتلاع الأخطاء:** إظهار فشل العمليات للمستخدم وتمييز 4xx الدائمة (خروج من الطابور بحالة «فشل نهائي») عن أخطاء الشبكة، مع حد أقصى وbackoff | H24, H33 |
| 0.10 | **إنشاء CI أولي:** `supabase db lint` + `supabase test db` + `flutter analyze` + `flutter test` عند كل PR — قاعدة «لا إطلاق بلا بوابة» | K26 |

### المرحلة 1 — جاهزية تجريبي مغلق (5–10 محلات وفق `First-version.md:1574-1580`)

**الهدف:** ثقة تشغيلية: اختبارات نفي، مزامنة ثنائية الاتجاه، مراقبة، ونسخ احتياطي.
**الجهد التقديري:** ~3–4 أسابيع إضافية.

| # | البند | البنود المرتبطة |
|---|---|---|
| 1.1 | اختبارات pgTAP سلبية: تحويل كل خلية «مرفوض» في `docs/rls-matrix.md` إلى اختبار (عزل المستأجرين، عميل/عميل، أدوار) | K24, H43 |
| 1.2 | اختبارات وحدة لمنطق المزامنة والطابور وidempotency في الموبايل + اختبارات Deno للدوال الحرجة (التشفير، verify-statement، finalize-upload) | K25 |
| 1.3 | إكمال المزامنة ثنائية الاتجاه: سحب `ledger_entries` و`customer_currency_balances` والنزاعات، تحديث `sync_status` بعد النجاح، معالجة `reversal` في الرصيد التفاؤلي، استعلام مجمّع بدل N+1 | H19, H21, H32 |
| 1.4 | إصلاح `process-automation-rules` (أعمدة `reminders` الصحيحة، `dispute_state`، `enqueue_notification`) أو تعطيله صراحة وتوثيقه | K23 |
| 1.5 | تشديد تخويل الرفع إلى فحص دور كتابة + إزالة `image/svg+xml` من `business-assets` + فحص magic bytes | H9, H10 |
| 1.6 | إصلاح تسابق العكس/الإنشاء (قفل صف `business_customers` في العكس) ولقطة كشف الحساب (قفل أو عزل أعلى) وإكمال idempotency للأوامر الخمسة الناقصة | H1, H2, H3 |
| 1.7 | حسم سياسة «الدفع الأكبر من الرصيد» وتوحيد الوثائق والتنفيذ والاختبارات | H41 |
| 1.8 | خطة نسخ احتياطي + اختبار استعادة موثق + مراقبة (Crashlytics/Sentry + تنبيهات دوال وcron) | H45, H46 |
| 1.9 | حسم مسار OTP الإنتاجي (مزود موثق، أسرار، اختبار طرف-لطرف) + حماية `WORKER_SECRET` وإصلاح rate limit في `verify-statement` | H44, H11 |
| 1.10 | حماية البيانات المحلية: تشفير SQLite، منع حذف الطابور عند signOut قبل المزامنة (أو تحذير صريح)، `now()` من السيرفر لتواريخ القيود | H23, H25, H26 |
| 1.11 | بيانات بذرة لبيئة staging + تشغيل المرحلة التجريبية المغلقة مع فحص صحة يومي آلي (مطابقة `business_customer_balances` مقابل مجموع القيود) | 10 توصية 4 |

### المرحلة 2 — إطلاق تجاري

**الهدف:** اكتمال المنتج وظيفياً وتوثيقياً وتشغيلياً.
**الجهد التقديري:** ~4–6 أسابيع إضافية (تتضمن تشغيل التجريبي المغلق بنجاح).

| # | البند | البنود المرتبطة |
|---|---|---|
| 2.1 | تنفيذ واجهة العميل فعلياً: ربط `customer_business_summary` و`confirm_ledger_entry` و`respond_link_request` و`open_dispute` وإدراج أوامر العميل في الطابور | K16, H32 |
| 2.2 | ربط Edge Functions بالواجهة: `generate-statement` (PDF حقيقي وختم SHA-256)، `verify-statement` (شاشة تحقق)، `bootstrap-user-contact`/`customer-directory` (ربط العملاء بالهاتف)، رفع المرفقات عبر `signed-document-upload`/`finalize-document-upload` | K21, H28 |
| 2.3 | حل مشكلة عربية PDF (تضمين خط عربي TTF + دعم RTL/تشكيل) وإصلاح `Math.abs` في عمود الرصيد الجاري | H36 |
| 2.4 | حسم تعدد العملات نهائياً: إما إزالة الواجهة أو تنفيذ كامل (عملة في journal lines، تجميع الـ views والكشوفات حسب العملة، مزامنة الأرصدة، عرض مجزأ بدل الإجمالي المختلط، استبدال `'ر.ي'` الثابتة) | K22, H17–H20 |
| 2.5 | اختبارات تزامن وأداء وعدم قابلية تعديل + استكمال سيناريوهات النزاعات + اختبارات تعدد العملات | H16, H38–H40, H42 |
| 2.6 | تحديث التوثيق الشامل: `api-reference` و`database-architecture-spec` و`edge-functions.md` و`erd.md` و`full_schema.sql` (إعادة توليد آلياً) و`mobile/README.md` + اختبار pgTAP يقارن المخطط الفعلي بالمرجع في CI | H34, H35 |
| 2.7 | سد الثغرات المتوسطة المتراكمة: revoke صريح من PUBLIC/anon لكل دالة جديدة، توحيد سياسات `link_status`، `cleanup-expired-data` أو منظّف `upload_sessions`، منع الفترات المفتوحة المتداخلة، `updated_at` لـ `sync_checkpoints`، حالة `voided` (قيد عكسي أو استثناء)، مصالحة دورية للأرصدة | H4–H8, H37 |
| 2.8 | توثيق تشغيلي (runbook): استعادة، تدوير أسرار، نزاع مصعد، إيقاف حساب مسيء + feature flags للتجريبي | 10 توصية 6 |

---

## 6. نقاط القوة — ما هو مبني باحترافية ويستحق الثقة

رغم 212 ملاحظة سلبية، سجّلت التقارير **75 نقطة إيجابية** تؤكد أن النواة سليمة وقابلة للبناء عليها:

### قاعدة البيانات والمخطط (تقارير 01، 02، 05)
- **لا فاصلة عائمة إطلاقاً:** كل المبالغ `numeric(20,4)` — دقة كاملة بلا تقريب (0001:129,165,350-353؛ 0010:83-84).
- **سجل مالي append-only حقيقي:** تريجرات `prevent_update_delete` على كل جداول الوقائع المالية السبعة (0001:442-452).
- **توازن القيد المزدوج مفروض قاعدياً:** constraint trigger مؤجل `trg_journal_entry_balanced` (0010:204-221) يمنع commit أي قيد غير متوازن — ضمان على مستوى قاعدة البيانات لا التطبيق، مع منع القيود اليتيمة (0010:223-237) وذرّية كاملة (مستحيل قيد تشغيلي بلا يومية).
- **الترحيلات المحاسبية صحيحة نوعاً نوعاً** (افتتاحي/دين/سداد/خصم/عكس) — تحقق يدوي مطابق للمواصفة والاختبار (05§أولاً).
- **Idempotency بطبقتين:** `unique(business_id, client_request_id)` (0001:176) + `command_receipts` (0006:19-28) مع فحص مسبق بعد قفل صف العميل — إعادة التنفيذ آمنة حتى تحت التزامن.
- **سلامة العكس:** منع عكس العكس، منع العكس المزدوج، إلزام تطابق المبلغ والعملة وعكس الاتجاه (0010:335-341).
- **نمط أوامر سليم:** `private.command_*` بـ `security definer` + `set search_path=''` + أغلفة عامة رفيعة مع دفاع بالعمق عبر `validate_ledger_entry_insert`.
- **Outbox تعاملي احترافي:** إشعار + outbox في نفس المعاملة، سحب بـ `FOR UPDATE SKIP LOCKED`، تراجع أسّي، dead-letter بعد 8 محاولات (0002:88-111؛ 0005:46-77؛ 0006:216-239).

### الأمان (تقرير 03)
- **RLS مفعّل على كل الجداول العامة الـ 31 بلا استثناء** (مطابقة كاملة `create table` ↔ `enable row level security`).
- **Default-deny منهجي:** سحب شامل ثم منح SELECT فقط، وكل الكتابات عبر RPCs محصّنة (0003:30-53).
- **عزل PII الهاتف:** تشفير AES-256-GCM + HMAC-SHA256 للبحث في `private.customer_contacts` (0001:80-91) — بنية صحيحة بتطبيع E.164 صارم.
- **كل الـ views الخمسة بـ `security_invoker=true`** — لا تجاوز لـ RLS (0002:653-679؛ 0010:465-480).
- **Storage منضبط:** باقات خاصة افتراضياً، الرفع عبر signed URL بعد فحص وصول، حدود حجم وأنواع MIME، جلسات رفع تنتهي خلال 15 دقيقة وتُستهلك مرة واحدة.
- **Realtime محدود النطاق** — لا جداول PII ولا قيود خام (0004:99-117)؛ عمال الخلفية محميون بـ `requireWorkerSecret` وCORS مقيد.

### Edge Functions (تقرير 04)
- `requireUser` يتحقق من JWT فعلياً عبر `auth.getUser` في 7 من 9 دوال؛ فصل صلاحيات least-privilege منضبط.
- rate limiting مركزي ذرّي صحيح تزامنياً (`service_consume_rate_limit`)؛ قواعد الأتمتة idempotent عبر `run_key`؛ حسابات الكشف في قاعدة البيانات سليمة رياضياً (رصيد افتتاحي + مدين − دائن بترتيب حتمي).

### الموبايل (تقرير 07)
- **مبدأ API-only مطبَّق فعلياً على كل العمليات المالية** — لا كتابة مباشرة على الجداول المحصّنة (الاستثناء الوحيد `profiles.upsert`).
- **الحفظ التفاؤلي + إدراج الطابور في معاملة SQLite واحدة** — لا قيد محلي بلا أمر مرتبط (app_database.dart:371-474).
- حارس تزامن `_isProcessing` وطابور FIFO وبنية Repository/Database/SyncEngine قابلة للصيانة.

### التوثيق والاختبارات (تقارير 09، 10)
- **كل RPCs الموثقة في مرجع API منفذة فعلاً بنفس الأسماء والمعاملات والأدوار** (09 P-1)، والمبادئ المالية غير القابلة للتنازل منفذة كما وُثقت (P-2)، ومصفوفة RLS الموثقة تطابق التنفيذ (P-3).
- **31 اختبار pgTAP جيدة الصنع:** حتمية، بلا تلويث بيانات، تحاكي مستخدمين حقيقيين تحت RLS، وتغطي المسار السعيد الكامل بما فيه idempotency والنزاع الجزئي والقيد المزدوج المتوازن.
- `.env.example` موثق بعناية بلا أسرار حقيقية، و`config.toml` يعطل الدخول المجهول ويفرض كلمة مرور ≥ 12.

---

## 7. خاتمة

الصورة النهائية متسقة عبر التقارير العشرة: **نواة خلفية من الطراز الأول (0001–0011) محاصرة بطبقة أخيرة مكسورة (0012)، وواجهة أمامية غير موصولة فعلياً بالباكند، ومصادقة شكلية، وسر مسرّب، وغياب بوابات جودة.** لا توجد ملاحظة حرجة واحدة غير قابلة للإصلاح — كلها ذات طبيعة «أسابيع عمل» لا «إعادة تصميم». بتنفيذ المرحلة صفر (~2–3 أسابيع) يصبح النظام قابلاً للنشر والكتابة الحقيقية؛ وبالمرحلتين 1 و2 يصل إلى جاهزية تجارية كاملة. أول إجراءين اليوم: **تدوير مفتاح OpenWA** (ساعات) و**تجميد الترحيل 0012** (قرار إداري فوري).

---

*أُعدّ هذا التقرير بدمج محتوى التقارير العشرة في `analysis-reports/` حصراً دون إضافة ملاحظات جديدة؛ كل مراجع الملفات وأرقام الأسطر منقولة من تلك التقارير وقابلة للتحقق فيها.*
